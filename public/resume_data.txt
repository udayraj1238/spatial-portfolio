========================================================
  UDAY RAJ - APEX AI KNOWLEDGE BASE
  Last Updated: 2026-06-27 19:50
  Source: PDF Resume + GitHub Code Analysis + LinkedIn
========================================================

== SECTION 1: RESUME (Primary Source) ==
Uday Raj
 +91 8527655484
# rajuday6002@gmail.com
ï LinkedIn
§ GitHub
 Portfolio
Education
Indian Institute Of Information Technology, Design and Manufacturing
Kurnool
Bachelor of Technology in Artificial Intelligence and Data Science - Grade : 8.38
2024 - 2028
Delhi Public School Sushant Lok
Gurugram
High School - (Physics, Chemistry, Mathematics) - Grade : 95%
2022 - 2024
Relevant Coursework
• Data Structures
• Algorithms Analysis
• Computer Vision
• Artificial Intelligence
• Machine Learning
• Deep Learning
• Statistical Analysis
• Discrete Mathematics
Experience
SVNIT Surat
Nov 2025 - Apr 2026
Research Summer Intern - Department of Artificial Intelligence
Surat, India
• Designed TransEvade under Dr. Praveen Kumar Chandaliya, a printability-constrained attention-hijacking attack for
transformer-based Person Re-ID systems.
• Engineered a SegFormer/MiT Re-ID pipeline across Market-1501, DukeMTMC-reID, and MSMT17 using MiT-B0/B1
backbones, 256/512-D descriptors, and cosine retrieval.
• Optimized 500-1000 step adversarial pattern generation with Adam, ImageNet normalization, chest/waist/legs masks,
and distract-suppress attention losses.
• Quantified 90-identity spatial ablations where torso patches reduced adversarial Rank-1 to 1.67%-7.04% on Duke/MSMT,
exposing attention priors.
Projects
CourtSense AI - 3D Tennis Analytics Engine
[GitHub]
• Created an end-to-end sports CV pipeline converting tennis videos into 150-frame 3D replay clips with FastAPI, React,
Three.js, and JSON coordinate streams.
• Combined YOLOv8-Pose, SegFormer-B2, OpenCV homography, and HSV/YOLO ball tracking to map 17 COCO
keypoints onto court coordinates.
• Applied Kalman filtering, EMA smoothing (alpha = 0.3), 5-frame rolling stats, Magnus-effect correction, km/h speed,
and rpm spin estimation.
PaliGemma Vision-Language Model From Scratch
[GitHub]
• Recreated a PaliGemma-style VLM in PyTorch by pairing a 12-layer SigLIP vision transformer, 16x16 patch embeddings,
196 visual tokens, and Gemma decoder.
• Programmed a 768-to-2048 multimodal projector, image-token merging at index 256000, RMSNorm, RoPE, grouped KV
attention, and 8192-token context support.
• Enabled KV-cache and causal masking for autoregressive multimodal generation across modular SigLIP, Gemma, and
PaliGemma components.
Technical Skills
Languages: Python, C, C++, SQL, HTML
Machine Learning Stack: PyTorch, TensorFlow, Keras, torchvision, OpenCV, scikit-learn
Computer Vision & Deep Learning: YOLOv8, SegFormer, ViT, SigLIP, Person Re-ID, Transformers, NumPy, Pandas
MLOps & Deployment: FastAPI, REST API, Docker, CUDA, Linux, Git, GitHub, Jupyter
Achievements
• Placed global top 20 in the [Shell.ai] Hackathon among 1000+ submissions, optimizing ML model parameters.
• Ranked 176th out of 7000+ registrants in [AWS x Zelestra ML Ascend Hackathon].
• Earned a 3-star rating of 1630 on [CodeChef] and solved 100+ DSA problems on [LeetCode] and [Code360].
Positions of Responsibility
Google Developer Groups
Machine Learning Coordinator
Aug 2025 - Present
• Conducted a 12-week ML bootcamp for 200+ students, covering SGD, CNNs, and Kaggle workflows, resulting in 15+
student projects.
DataWorks Club
Computer Vision Coordinator
Aug 2025 - Present
• Mentored 100+ members through OpenCV and PyTorch workshops, building real-time CV pipelines with YOLOv8 and
TensorRT targets of 30+ FPS.

Uday Raj
(cid:131) +91 8527655484 # rajuday6002@gmail.com (cid:239) LinkedIn § GitHub (cid:128) Portfolio
Education
Indian Institute Of Information Technology, Design and Manufacturing Kurnool
Bachelor of Technology in Artificial Intelligence and Data Science - Grade : 8.38 2024 - 2028
Delhi Public School Sushant Lok Gurugram
High School - (Physics, Chemistry, Mathematics) - Grade : 95% 2022 - 2024
Relevant Coursework
• Data Structures • Computer Vision • Machine Learning • Statistical Analysis
• Algorithms Analysis • Artificial Intelligence • Deep Learning • Discrete Mathematics
Experience
SVNIT Surat Nov 2025 - Apr 2026
Research Summer Intern - Department of Artificial Intelligence Surat, India
• Designed TransEvade under Dr. Praveen Kumar Chandaliya, a printability-constrained attention-hijacking attack for
transformer-based Person Re-ID systems.
• Engineered a SegFormer/MiT Re-ID pipeline across Market-1501, DukeMTMC-reID, and MSMT17 using MiT-B0/B1
backbones, 256/512-D descriptors, and cosine retrieval.
• Optimized 500-1000 step adversarial pattern generation with Adam, ImageNet normalization, chest/waist/legs masks,
and distract-suppress attention losses.
• Quantified 90-identity spatial ablations where torso patches reduced adversarial Rank-1 to 1.67%-7.04% on Duke/MSMT,
exposing attention priors.
Projects
CourtSense AI - 3D Tennis Analytics Engine [GitHub]
• Created an end-to-end sports CV pipeline converting tennis videos into 150-frame 3D replay clips with FastAPI, React,
Three.js, and JSON coordinate streams.
• Combined YOLOv8-Pose, SegFormer-B2, OpenCV homography, and HSV/YOLO ball tracking to map 17 COCO
keypoints onto court coordinates.
• Applied Kalman filtering, EMA smoothing (alpha = 0.3), 5-frame rolling stats, Magnus-effect correction, km/h speed,
and rpm spin estimation.
PaliGemma Vision-Language Model From Scratch [GitHub]
• Recreated a PaliGemma-style VLM in PyTorch by pairing a 12-layer SigLIP vision transformer, 16x16 patch embeddings,
196 visual tokens, and Gemma decoder.
• Programmed a 768-to-2048 multimodal projector, image-token merging at index 256000, RMSNorm, RoPE, grouped KV
attention, and 8192-token context support.
• Enabled KV-cache and causal masking for autoregressive multimodal generation across modular SigLIP, Gemma, and
PaliGemma components.
Technical Skills
Languages: Python, C, C++, SQL, HTML
Machine Learning Stack: PyTorch, TensorFlow, Keras, torchvision, OpenCV, scikit-learn
Computer Vision & Deep Learning: YOLOv8, SegFormer, ViT, SigLIP, Person Re-ID, Transformers, NumPy, Pandas
MLOps & Deployment: FastAPI, REST API, Docker, CUDA, Linux, Git, GitHub, Jupyter
Achievements
• Placed global top 20 in the [Shell.ai] Hackathon among 1000+ submissions, optimizing ML model parameters.
• Ranked 176th out of 7000+ registrants in [AWS x Zelestra ML Ascend Hackathon].
• Earned a 3-star rating of 1630 on [CodeChef] and solved 100+ DSA problems on [LeetCode] and [Code360].
Positions of Responsibility
Google Developer Groups
Machine Learning Coordinator Aug 2025 - Present
• Conducted a 12-week ML bootcamp for 200+ students, covering SGD, CNNs, and Kaggle workflows, resulting in 15+
student projects.
DataWorks Club
Computer Vision Coordinator Aug 2025 - Present
• Mentored 100+ members through OpenCV and PyTorch workshops, building real-time CV pipelines with YOLOv8 and
TensorRT targets of 30+ FPS.

== SECTION 2: LINKEDIN ==
================================================================
  UDAY RAJ — LINKEDIN PROFILE DATA
  URL: https://www.linkedin.com/in/uday6002/
  Last Updated: June 2026
  (Update this file whenever your LinkedIn changes, then push to git)
================================================================

HEADLINE:
AI & Computer Vision Engineer | PyTorch · YOLOv8 · Transformers · SegFormer | Ex-Research Intern @ SVNIT | Open to ML/CV Roles | IIITDM Kurnool

OPEN TO WORK:
Currently open to: ML Engineer, Computer Vision Engineer, AI Research roles

================================================================
ABOUT / SUMMARY
================================================================
I build AI systems that work — from adversarial machine learning research to real-time computer vision pipelines deployed with FastAPI and Docker.

Most recently, I completed a Research Internship at SVNIT Surat (Sardar Vallabhbhai National Institute of Technology), where I worked on adversarial robustness research in transformer-based computer vision systems under Dr. Praveen Kumar Chandaliya. I designed and evaluated adversarial attacks on deep learning models used in real-world vision tasks — exploring how these systems can be made more robust and secure. Paper currently under review.

On the project side:
- Built CourtSense AI — an end-to-end sports CV pipeline that converts tennis match videos into 150-frame interactive 3D replays using YOLOv8-Pose, SegFormer-B2, OpenCV homography, and Kalman filtering, served through a FastAPI backend and a React/Three.js frontend.
- Implemented PaliGemma (Google's Vision-Language Model) entirely from scratch in PyTorch — 12-layer SigLIP vision transformer, Gemma decoder, multimodal projector, grouped KV attention, RoPE, KV-cache — the full architecture with 8192-token context support.
- Built a Cognitive Engine for Grid07 using LangGraph orchestration, RAG combat pipelines, and multi-layered prompt injection defense.

Stack: Python, PyTorch, TensorFlow, OpenCV, YOLOv8, SegFormer, SigLIP, Transformers, FastAPI, Docker, CUDA, React.

Achievements:
- Global Top 20 in the Shell.ai Hackathon (1000+ submissions)
- Ranked 176th out of 7000+ in the AWS x Zelestra ML Ascend Hackathon
- 3-star rating (1630) on CodeChef, 100+ DSA problems on LeetCode & Code360

As ML Coordinator at Google Developer Groups, led a 12-week bootcamp training 200+ students on SGD, CNNs, and Kaggle workflows.

================================================================
EXPERIENCE
================================================================

RESEARCH INTERNSHIP — SVNIT Surat (Sardar Vallabhbhai National Institute of Technology)
Under: Dr. Praveen Kumar Chandaliya
Topic: Adversarial robustness of transformer-based computer vision systems
- Designed and evaluated adversarial attack strategies on vision transformer architectures
- Evaluated across standard benchmarks (Market-1501) using PyTorch and SegFormer
- Paper currently under review — code and detailed results to be released upon publication
Duration: Nov 2025 – Present

MACHINE LEARNING COORDINATOR — Google Developer Groups (GDG) On Campus
Part-time | Aug 2025 – Present | Kurnool, Andhra Pradesh
- Designed and conducted a 12-week ML bootcamp for 200+ students covering SGD, CNNs, data preprocessing, and end-to-end Kaggle competition workflows
- Mentored 15+ student project teams through the complete ML pipeline — from data collection and feature engineering to model training, evaluation, and deployment
- Created hands-on workshop materials on PyTorch fundamentals, transfer learning, and model optimization techniques

COMPUTER VISION COORDINATOR — DataWorks Club IIITDM Kurnool
Part-time | Aug 2025 – Present | Kurnool, Andhra Pradesh
- Mentored 100+ members through hands-on OpenCV and PyTorch workshops focused on building real-time computer vision pipelines
- Led workshops on YOLOv8 object detection, achieving TensorRT inference optimization targets of 30+ FPS on real-time video streams
- Built and delivered curriculum covering image processing, feature extraction, pose estimation, and model deployment with Docker

================================================================
EDUCATION
================================================================

IIITDM Kurnool — B.Tech, Artificial Intelligence and Data Science
Jul 2024 – May 2028 | CGPA: 8.38/10
Activities: ML Coordinator (GDG), CV Coordinator (DataWorks Club)
Achievements: Global Top 20 Shell.ai Hackathon, 176th/7000+ AWS Hackathon
Coursework: Machine Learning, Deep Learning, Computer Vision, AI, DSA, Statistical Analysis, Discrete Mathematics

Delhi Public School, Sushant Lok (DPS Gurugram)
High School | Physics, Chemistry, Mathematics | Grade: 95%
Mar 2022 – Mar 2024
Competitive Programming: CodeChef 3-star (1630 rating), 100+ LeetCode/Code360 problems

================================================================
PROJECTS
================================================================

ADVERSARIAL ROBUSTNESS RESEARCH — Transformer-based CV Systems
Nov 2025 – Present | SVNIT Surat Internship
Research on adversarial robustness conducted under Dr. Praveen Kumar Chandaliya.
Designed adversarial attack strategies on SegFormer and ViT architectures on Market-1501.
Paper under review — code to be released on publication.

APEX AI — AI-Powered Portfolio with Custom Chatbot
May 2026 | https://udayraj1238.vercel.app
Interactive AI-powered portfolio with APEX chatbot trained on resume, GitHub repos, and research.
Built with Next.js, TypeScript, powered by Groq LLaMA 3.3 70B / DeepSeek R1.
Features sci-fi terminal UI, live GitHub sync, markdown rendering, semantic search.

COURTSENSE AI — 3D Tennis Analytics Engine
Mar 2026 – May 2026 | https://github.com/udayraj1238/CourtSense-AI
End-to-end sports CV pipeline: raw tennis videos → interactive 150-frame 3D replays.
- YOLOv8-Pose: 17-keypoint skeleton extraction per frame
- SegFormer-B2: court surface segmentation
- OpenCV homography: 2D video → 3D court coordinate mapping
- HSV + YOLO dual-method ball tracking
- Kalman filtering + EMA smoothing (alpha=0.3): reduced 3D keypoint jitter by ~70%
- Magnus-effect correction: physics-accurate speed (km/h) and spin (rpm) estimation
- FastAPI backend + React/Three.js 3D visualization frontend
- 100% client-side video processing with MediaPipe PoseLandmarker
Demo: https://lnkd.in/gWEWpEij

PALIGEMMA — Vision-Language Model from Scratch in PyTorch
Dec 2025 | https://github.com/udayraj1238/Pytorch_PaliGemma
Complete from-scratch PyTorch implementation of Google's PaliGemma VLM:
- 12-layer SigLIP vision transformer with 16×16 patch embeddings (196 visual tokens)
- Gemma language decoder
- 768-to-2048 multimodal projector
- RMSNorm, Rotary Position Embeddings (RoPE)
- Grouped KV attention, KV-cache for efficient autoregressive generation
- Causal masking, 8192-token context windows
- No HuggingFace, no pre-built modules — pure PyTorch

================================================================
SKILLS
================================================================
Python, PyTorch, TensorFlow, Machine Learning, Computer Vision, Adversarial ML,
SegFormer, YOLOv8, OpenCV, SigLIP, Transformers, Deep Learning, NLP,
FastAPI, Docker, CUDA, React, NumPy, Pandas, Scikit-Learn, Git, Linux,
CNN, GANs, LangGraph, RAG, Data Structures, C++

================================================================
RECENT LINKEDIN POSTS (reflecting personality & learning style)
================================================================

POST 1 (1 day ago — EY Biodiversity Challenge):
"I climbed to #2 on the public leaderboard... and then finished 26th. Here is what happened."
Key takeaways shared:
1. Public LB score ≠ Model quality — dangerous false sense of security
2. Row probing is risky — boosts public rank but destroys private generalization
3. Diversify final submissions — don't choose two nearly identical files
4. Trust your CV — robust cross-validation matters infinitely more than leaderboard chasing
"Never let the public leaderboard become your training set."
[25 reactions, 849 impressions]

POST 2 (6 days ago — CodeChef 3-star):
"I just hit the 3-Star rating on CodeChef (Rating: 1630)!"
"Building AI systems requires solid engineering fundamentals, and CP has been the perfect training ground."
[42 reactions, 1,102 impressions]

POST 3 (1 week ago — CourtSense AI):
"I built a computer vision pipeline that turns tennis match videos into interactive 3D replays — and deployed it live."
"Combining classical methods (Kalman filter, homography, EMA smoothing) with modern deep learning models gave dramatically better results than trying to solve everything with a single neural network. The Kalman + EMA combo alone reduced 3D keypoint jitter by ~70%."
[50 reactions, 1,333 impressions]

================================================================
END OF LINKEDIN DATA
Update this file and push to git whenever your LinkedIn changes.
The GitHub Actions workflow will automatically rebuild the AI brain.
================================================================

== SECTION 3: GITHUB - DEEP CODE ANALYSIS ==
(The following is extracted directly from actual source code in each repository.
 This includes imports, classes, functions, docstrings, and architecture patterns.)



=== REPO: spatial-portfolio ===
URL: https://github.com/udayraj1238/spatial-portfolio
Description: AI-powered portfolio with APEX chatbot trained on resume, GitHub and research. Next.js, TypeScript, Groq LLaMA 3.3 70B
Primary Language: TypeScript | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: README.md ---
# 🚀 Uday Raj's AI Portfolio — Interactive 3D & APEX AI Assistant

[![Vercel](https://img.shields.io/badge/Deployed-Vercel-black?style=flat-square&logo=vercel)](https://udayraj1238.vercel.app)
[![Next.js](https://img.shields.io/badge/Next.js-16-black?style=flat-square&logo=next.js)](https://nextjs.org)
[![React](https://img.shields.io/badge/React-19-61DAFB?style=flat-square&logo=react)](https://react.dev)
[![TypeScript](https://img.shields.io/badge/TypeScript-5-3178C6?style=flat-square&logo=typescript)](https://www.typescriptlang.org)
[![AI](https://img.shields.io/badge/AI-Groq%20%7C%20LLaMA-FF6B35?style=flat-square)](https://groq.com)

## ✨ Features

- **Interactive 3D Scene** — React Three Fiber with orbital rings, particle systems, and mouse-reactive camera
- **APEX AI Assistant** — Chat with an AI trained on my complete resume, GitHub, and research papers
- **Real-Time Streaming** — Edge runtime for instant, low-latency responses (Groq LLaMA 3.3 70B)
- **Voice Support** — Speech-to-text and text-to-speech for hands-free interaction
- **Production-Grade Security** — Rate limiting, input validation, error boundaries, environment variable management
- **Lightning Fast** — Optimized bundle, cached AI responses, edge deployment

## 🎯 Quick Start

### Prerequisites
- Node.js 18+ 
- npm or yarn

### Installation

```bash
# Clone the repo
git clone https://github.com/udayraj1238/spatial-portfolio.git
cd spatial-portfolio

# Install dependencies
npm install

# Setup environment (see .env.example)
cp .env.example .env.local
# Add your API keys to .env.local

# Run dev server
npm run dev
```

Visit `http://localhost:3000`

## 🔧 Build & Deploy

```bash
# Production build
npm run build

# Start production server
npm start
```

**Deploy to Vercel:**
```bash
npx vercel
```

## 📚 Project Structure
src/
├── app/
│   ├── page.tsx              # Main landing page
│   ├── layout.tsx            # Root layout with error boundary
│   ├── globals.css           # Global styles
│   └── api/
│       └── chat/route.ts     # APEX AI endpoint (streaming + rate limiting)
├── components/
│   ├── ChatTerminal.tsx      # AI chat UI with voice support
│   ├── ErrorBoundary.tsx     # React error boundary
│   └── canvas/
│       ├── Scene.tsx         # Main 3D scene
│       ├── HeroScene.tsx     # Hero section
│       ├── BentoGrid.tsx     # Layout component
│       └── ScrollCamera.tsx  # Camera controller
├── data/
│   └── knowledge.ts          # AI system prompt and knowledge base
└── helpers/
├── rateLimit.ts          # Rate limiting middleware
└── tunnel.ts             # Portal renderer

## 🤖 APEX AI

APEX is an AI assistant trained on:
- **Resume** — Complete education, experience, and skills
- **GitHub** — All 6 major projects with technical breakdowns
- **Research** — Adversarial ML, Computer Vision, LLMs

Ask APEX anything:
- ⚔️ "Tell me about his adversarial attack research"
- 🧠 "Explain the PaliGemma multimodal implementation"
- 🏆 "What are his competition rankings?"
-

--- File: generate_sound.py ---
IMPORTS (Libraries used):
import wave
import struct
import math
import os

KEY CODE COMMENTS:
  # Ensure public directory exists
  # Sine wave with exponential decay

--- File: update_resume_ai.py ---
IMPORTS (Libraries used):
import os, sys, re, json, base64, time
import urllib.request, urllib.error
import pdfplumber
from datetime import datetime

FUNCTIONS/METHODS:
  def _read_token() -> str
  def gh_api(endpoint: str) -> dict | list | None
  def gh_file_content(owner: str, repo: str, path: str) -> str -- Fetch and decode a file's content from GitHub.
  def extract_python_insights(code: str, filepath: str) -> str -- Extract meaningful technical info from Python code.

KEY CONSTANTS/CONFIG:
  PORTFOLIO_DIR = os.path.dirname(os.path.abspath(__file__))
  RESUME_PDF    = os.path.join(PORTFOLIO_DIR, "resume_updated.pdf")
  OUTPUT_TXT    = os.path.join(PORTFOLIO_DIR, "public", "resume_data.txt")
  GITHUB_USER   = "udayraj1238"
  LINKEDIN_URL  = "https://www.linkedin.com/in/uday6002/"
  GITHUB_TOKEN = _read_token()
  CODE_EXTS = {'.py', '.ts', '.tsx', '.js', '.jsx', '.md', '.ipynb', '.yaml', '.yml'}
  SKIP_PATTERNS = {
  MAX_FILES_PER_REPO = 12   # Max files to analyze per repo
  MAX_CHARS_PER_FILE = 6000  # Max chars to read from each file

KEY CODE COMMENTS:
  # ── CONFIG ────────────────────────────────────────────────────────────────────
  # ── GitHub Token (NEVER hardcode this — keep it out of git) ──────────────────
  # Option A: Set env var in your terminal before running:
  # PowerShell:  $env:GITHUB_TOKEN = "ghp_yourtoken"
  # CMD:         set GITHUB_TOKEN=ghp_yourtoken
  # Option B: Add to Portfolio/.env.local (already gitignored):
  # GITHUB_TOKEN=ghp_yourtoken
  # 1. Try .env.local FIRST (user's personal token takes priority)
  # 2. Fall back to environment variable
  # Code extensions to analyze

--- File: AGENTS.md ---
<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

--- File: Uday_Raj_Amazon_MLSS_SOP.md ---
### Statement of Purpose: Amazon Machine Learning Summer School

I’ve always hated treating machine learning models like magic black boxes. As a second-year AI & Data Science student at IIITDM Kurnool, my biggest priority has been figuring out how things actually work under the hood. That’s what pushed me to build the original Transformer architecture entirely from scratch using pure PyTorch. I wanted to genuinely understand multi-head attention and causal masking, rather than just calling a HuggingFace API and hoping for the best.

That curiosity quickly spilled over into building full-scale projects. Recently, I built *CourtSense-AI*, a computer vision pipeline that takes 2D tennis match videos and turns them into 3D interactive replays. I used YOLOv8-Pose and SegFormer, but the real challenge was optimizing it with TensorRT to hit 30+ FPS on edge hardware. I also wanted to get my hands dirty with multimodal AI, so I built my own version of the PaliGemma vision-language model from the ground up—hooking up a SigLIP encoder to a Gemma-2B decoder and using 8-bit quantization to cut VRAM usage in half.

Right now, I’m working as a Research Intern at SVNIT Surat, focusing on the security side of AI. I recently discovered that if you execute a surgical adversarial attack on just the Stage-4 attention blocks of a SegFormer model, its accuracy absolutely crashes—from 97% down to 2% on the Market-1501 dataset. We call it a "Nuclear Attack," and the research paper is currently under review. But I don't just keep these things to myself; as the ML Coordinator for my campus GDG, I recently led a 12-week bootcamp where I taught over 200 students how to go from learning basic gradient descent to shipping real-world ML apps.

Despite everything I’ve built, there is still a massive gap in my knowledge that I’m hoping the Amazon MLSS can help me fill. I know how to train and deploy complex architectures on local GPUs and edge devices, but I have almost no experience scaling these models to a massive, distributed enterprise level. I want to learn Amazon’s actual industry standards for things like distributed training, automating robust MLOps pipelines, and handling incredibly large, noisy datasets in the cloud.

I believe I’d be a great fit for this program because I have a proven track record of diving into the deep end and learning fast. I’m the kind of developer who tears architectures apart to find their bottlenecks rather than just using them blindly. I would absolutely love the opportunity to absorb Amazon’s expertise in scalable AI, use it to push my research further, and eventually share those enterprise-grade insights with the students I mentor.

--- File: comprehensive_architecture_guide.md ---
# The Uday Raj Spatial AI Portfolio: Comprehensive Architectural Guide

This document serves as the absolute deep-dive into the architecture, features, experimental decisions, and underlying logic of the Spatial AI Portfolio. It is designed to make the complete workings of this project crystal clear, from the WebGL context down to the Vercel Edge Runtime LLM token management.

---

## 1. Introduction & Core Concept

Traditional software engineering and ML portfolios are static: a list of GitHub links, a PDF resume, and perhaps a basic React website. The goal of this project was to build a **living, breathing AI portfolio**. Instead of telling a recruiter that Uday Raj knows AI, the portfolio *shows* it by embedding a custom-engineered AI assistant ("APEX") directly into a highly interactive, 3D WebGL environment. 

The portfolio is split into two massive pillars:
1. **The Spatial Frontend:** A React Three Fiber (R3F) powered 3D universe that reacts to user interactions.
2. **The Intelligence Engine:** A Vercel Edge Runtime backend connecting to Groq's ultra-fast Llama 3 LLMs, bound by aggressive rate-limiting, dynamic token budgeting, and advanced prompt engineering.

---

## 2. The Tech Stack Breakdown

### Frontend Framework: Next.js 16 (App Router)
We chose Next.js 16 because of its robust App Router (`src/app`), excellent TypeScript support, and seamless integration with Vercel's Edge Functions. The App Router allows us to perfectly separate server-side API routes (`route.ts`) from client-side interactive components (`page.tsx`, `HeroScene.tsx`).

### 3D Graphics: React Three Fiber (R3F) & Three.js
Writing raw WebGL or Three.js code is notoriously verbose. R3F allows us to write 3D scenes declaratively using React components. We supplemented this with `@react-three/drei`, a collection of useful helpers (like `Stars`, `Float`, and `PerspectiveCamera`) that drastically reduced boilerplate code.

### AI Infrastructure: Vercel AI SDK & Groq API
Instead of building a custom WebSocket or Server-Sent Events (SSE) pipeline from scratch for AI streaming, we utilized the `@ai-sdk/react` library (`useChat`). This hook automatically manages the streaming state of AI responses. The backend uses the Groq API, which leverages custom LPU (Language Processing Unit) hardware to serve Llama 3 models at blistering speeds (up to 800 tokens per second), providing instant responses that make the AI feel truly alive.

---

## 3. The Spatial Frontend (The 3D Canvas)

### A. The WebGL Context Experiment (Bug 1)
**The Experiment:** Initially, we attempted to wrap the entire application in a global `<Canvas>` element inside `layout.tsx`. The idea was that the 3D background would persist seamlessly across page navigations.
**The Failure:** Mobile devices and laptops without dedicated GPUs began crashing. We realized that by having a Canvas in `layout.tsx` and another 3D component rendering in `page.tsx`, we accidentally created **two overlapping WebGL contexts**. Browser

--- File: final_comprehensive_architecture_guide.md ---
# The Uday Raj Spatial AI Portfolio: Comprehensive Architectural Guide

This document serves as the absolute deep-dive into the architecture, features, experimental decisions, and underlying logic of the Spatial AI Portfolio. It is designed to make the complete workings of this project crystal clear, from the WebGL context down to the Vercel Edge Runtime LLM token management.

---

## 1. Introduction & Core Concept

Traditional software engineering and ML portfolios are static: a list of GitHub links, a PDF resume, and perhaps a basic React website. The goal of this project was to build a **living, breathing AI portfolio**. Instead of telling a recruiter that Uday Raj knows AI, the portfolio *shows* it by embedding a custom-engineered AI assistant ("APEX") directly into a highly interactive, 3D WebGL environment. 

The portfolio is split into two massive pillars:
1. **The Spatial Frontend:** A React Three Fiber (R3F) powered 3D universe that reacts to user interactions.
2. **The Intelligence Engine:** A Vercel Edge Runtime backend connecting to Groq's ultra-fast Llama 3 LLMs, bound by aggressive rate-limiting, dynamic token budgeting, and advanced prompt engineering.

---

## 2. The Tech Stack Breakdown

### Frontend Framework: Next.js 16 (App Router)
We chose Next.js 16 because of its robust App Router (`src/app`), excellent TypeScript support, and seamless integration with Vercel's Edge Functions. The App Router allows us to perfectly separate server-side API routes (`route.ts`) from client-side interactive components (`page.tsx`, `HeroScene.tsx`).

### 3D Graphics: React Three Fiber (R3F) & Three.js
Writing raw WebGL or Three.js code is notoriously verbose. R3F allows us to write 3D scenes declaratively using React components. We supplemented this with `@react-three/drei`, a collection of useful helpers (like `Stars`, `Float`, and `PerspectiveCamera`) that drastically reduced boilerplate code.

### AI Infrastructure: Vercel AI SDK & Groq API
Instead of building a custom WebSocket or Server-Sent Events (SSE) pipeline from scratch for AI streaming, we utilized the `@ai-sdk/react` library (`useChat`). This hook automatically manages the streaming state of AI responses. The backend uses the Groq API, which leverages custom LPU (Language Processing Unit) hardware to serve Llama 3 models at blistering speeds (up to 800 tokens per second), providing instant responses that make the AI feel truly alive.

---

## 3. The Spatial Frontend (The 3D Canvas)

### A. The WebGL Context Experiment (Bug 1)
**The Experiment:** Initially, we attempted to wrap the entire application in a global `<Canvas>` element inside `layout.tsx`. The idea was that the 3D background would persist seamlessly across page navigations.
**The Failure:** Mobile devices and laptops without dedicated GPUs began crashing. We realized that by having a Canvas in `layout.tsx` and another 3D component rendering in `page.tsx`, we accidentally created **two overlapping WebGL contexts**. Browser

--- File: next-env.d.ts ---
IMPORTS:
import "./.next/types/routes.d.ts";

CODE COMMENTS:
/// <reference types="next" />
/// <reference types="next/image-types/global" />
// NOTE: This file should not be edited
// see https://nextjs.org/docs/app/api-reference/config/typescript for more information.

--- File: next.config.ts ---
IMPORTS:
import type { NextConfig } from "next";

FUNCTIONS/COMPONENTS:
const nextConfig: NextConfig = {

CODE COMMENTS:
// ━━━ Performance ━━━
// ━━━ Type Safety ━━━
// ━━━ Images ━━━
// ━━━ Headers & Security ━━━

--- File: scripts/ingest.js ---
FUNCTIONS/COMPONENTS:
const fs = require('fs');
const path = require('path');
const { createClient } = require('@supabase/supabase-js');
const SUPABASE_URL = process.env.NEXT_PUBLIC_SUPABASE_URL;
const SUPABASE_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY;
const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);
async function getEmbedding(text) {
const { pipeline } = await import('@xenova/transformers');
const result = await pipelineInstance(text, { pooling: 'mean', normalize: true });
function chunkText(text, maxChars = 1000) {
const paragraphs = text.split(/\r?\n\r?\n/);
const chunks = [];
async function main() {
const dataPath = path.join(__dirname, '..', 'public', 'resume_data.txt');
const text = fs.readFileSync(dataPath, 'utf-8');

CODE COMMENTS:
// We use a singleton pattern for the pipeline
// Simple chunking logic: split by double newlines, group up to ~1000 characters
// Clear existing knowledge base
// Embed and insert each chunk

--- File: src/app/layout.tsx ---
IMPORTS:
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ErrorBoundary } from "@/components/ErrorBoundary";

FUNCTIONS/COMPONENTS:
const geistSans = Geist({
const geistMono = Geist_Mono({
export const metadata: Metadata = {
export default function RootLayout({============================================================
=== REPO: DistroSync ===
URL: https://github.com/udayraj1238/DistroSync
Description: No description
Primary Language: Python | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: README.md ---
# DistroSync

A distributed task queue with adaptive load shedding — built from scratch in Python.

## What is this?

DistroSync is a ground-up implementation of a message broker and task queue system, similar to what Kafka, RabbitMQ, and Celery do under the hood. No libraries for the core — just raw Python sockets, asyncio, and multiprocessing.

## Architecture

- **Broker** — Central TCP server that accepts tasks from producers and dispatches them to workers
- **Producers** — Client programs that submit tasks to the broker
- **Workers** — Processes that pull tasks and execute them

## Tech Stack

- Python 3.10+
- asyncio (non-blocking I/O)
- Raw TCP sockets (length-prefixed JSON protocol)
- SQLite / WAL mode (crash-safe persistence)
- multiprocessing (CPU-bound task execution)
- FastAPI (observability dashboard)
- Docker (containerized deployment)

## Status

🚧 Under active development

--- File: broker/dead_letter.py ---
IMPORTS (Libraries used):
import asyncio
import time
import logging
from collections import OrderedDict
from typing import Optional, List

CLASSES:
  class DeadLetterQueue:: In-memory Dead Letter Queue for permanently failed tasks.  Tasks land here after exhausting their retry budget. Each task retains its full history: original payload, queue name, attempt count, and the timestamp when it was dead-lettered.  The DLQ is implemented as an OrderedDict keyed by task_id so that tasks maintain insertion order (useful for FIFO inspection) and can be looked up by ID in O(1) for individual operations.  Thread safety: protected by an asyncio.Lock since the broker's event loop, the NACK handler, and any future admin API all access the DLQ concurrently.

FUNCTIONS/METHODS:
  def __init__(self)
  async def add(self, task) -> None -- Add a permanently failed task to the DLQ.  Called by QueueManager.negative_acknowledge() when a task has exceeded its maximum retry count.
  async def peek(self, limit: int = 10) -> List[dict] -- Inspect tasks in the DLQ without removing them.  Returns a list of task summaries in insertion order (oldest first).
  async def remove(self, task_id: str) -> bool -- Remove a specific task from the DLQ.  Used after manual inspection/processing of a failed task.
  async def retry(self, task_id: str, queue_manager) -> bool -- Move a task from the DLQ back to its original queue for retry.  This is used when an operator has fixed the underlying issue (e.g., patched a bug, restored a downstream service) and wants
  async def purge(self) -> int -- Remove ALL tasks from the DLQ.  Use w

KEY CODE COMMENTS:
  # OrderedDict preserves insertion order AND allows O(1) lookup
  # Reset the task for a fresh retry cycle

--- File: broker/http_api.py ---
IMPORTS (Libraries used):
import asyncio
import json
import logging
import os
from typing import Optional, Callable

CLASSES:
  class HTTPAPIServer:: Minimal async HTTP server for the DistroSync dashboard and metrics.  Usage: http_server = HTTPAPIServer( host="0.0.0.0", port=8000, metrics_handler=broker.get_metrics_snapshot, dlq_handler=broker.get_dlq_listing, ) await http_server.start()

FUNCTIONS/METHODS:
  def __init__(
  async def handle_connection(
  async def _send_response(
  async def _send_html_response(

KEY CONSTANTS/CONFIG:
  DASHBOARD_DIR = os.path.join(

KEY CODE COMMENTS:
  # Path to the dashboard HTML file (relative to project root)
  # Read the full HTTP request (up to 8KB — more than enough for GET)
  # Parse the request line: "GET /path HTTP/1.1"
  # Only support GET

--- File: broker/load_shedder.py ---
IMPORTS (Libraries used):
import asyncio
import time
import logging
from dataclasses import dataclass, field
from typing import Dict

CLASSES:
  class TokenBucket:: A single token bucket for rate-limiting.  The bucket starts full (tokens = capacity). Tokens are consumed by accepted requests and refilled over time at the adjusted rate.  Why float tokens instead of int? Token refill is time-based. If 0.3 seconds elapsed at 100 tokens/sec, we add 30.0 tokens. Using int would truncate fractional tokens on every refill, causing drift over time.  Attributes: capacity:          Maximum tokens the bucket can hold. nominal_fill_rate: Tokens per second at zero load. tokens:            Current token count. last_refill:       Monotonic timestamp of the last refill.
  class AdaptiveLoadShedder:: Rate limiter that adjusts its fill rate based on system pressure.  The load shedder maintains one TokenBucket per queue. Each bucket's fill rate is adjusted every time a produce request comes in, based on the current queue depth and average worker latency.  Tuning parameters: BASE_FILL_RATE (100 tokens/sec): The nominal rate at zero load. 100 means the system can handle 100 tasks/sec per queue when things are healthy.  MIN_FILL_RATE (5 tokens/sec): Floor rate. Even under extreme load, we still allow some tasks through so the system doesn't completely starve. This prevents a total blackout during load spikes.  QUEUE_DEPTH_THRESHOLD (50): Start throttling when queue depth exceeds this. 50 is a good default for most workloads — it gives workers a buffer without letting the queue grow unbounded.  LATENCY_THRESHOLD_MS (200): Start throttling when average worker latency exceeds 200ms. This catches cases where the queue isn't deep yet but workers are clearly struggling (e.g., downstream service is slow).  BUCKET_CAPACITY (200): Max tokens in each bucket. This allows short b

FUNCTIONS/METHODS:
  def __post_init__(self)
  def refill(self, adjusted_rate: float) -- Add tokens based on time elapsed and the current adjusted rate.  Uses monotonic clock because wall clock (time.time) can jump backwards during NTP corrections, which would break the elapsed
  def consume(self) -> bool -- Try to consume one token from the bucket.  Returns: True if a token was available and consumed (request allowed).

--- File: broker/metrics_collector.py ---
IMPORTS (Libraries used):
import time
import threading
import logging
from collections import deque, defaultdict
from typing import Dict, Optional

CLASSES:
  class MetricsCollector:: Collects and exposes real-time broker metrics.  Usage: collector = MetricsCollector() collector.record_produce("emails")           # Task produced collector.record_consume("emails", task_id)  # Task dequeued collector.record_ack("emails", task_id)       # Task completed collector.record_nack("emails", task_id)      # Task failed  metrics = collector.snapshot()  # Get current metrics

FUNCTIONS/METHODS:
  def __init__(self)
  def _prune_window(self, timestamps: deque, now: float) -> None -- Remove timestamps older than the throughput window.
  def record_produce(self, queue_name: str) -> None -- Record that a task was produced (enqueued) to a queue.
  def record_consume(self, queue_name: str, task_id: str) -> None -- Record that a task was consumed (dequeued) from a queue.
  def record_ack(self, queue_name: str, task_id: str) -> None -- Record that a task was acknowledged (completed successfully).  Computes the processing latency if we have the dequeue timestamp.
  def record_nack(self, queue_name: str, task_id: str,
  def throughput(self, queue_name: str) -> float -- Get the current throughput for a queue in tasks/second.  Uses the ACK rate (completed tasks) as th

KEY CONSTANTS/CONFIG:
  THROUGHPUT_WINDOW_SECONDS = 10
  MAX_LATENCY_SAMPLES = 1000

KEY CODE COMMENTS:
  # How many seconds of history to keep for throughput calculation
  # Maximum number of latency samples to keep per queue
  # Per-queue event timestamps for throughput (deque of float timestamps)
  # Per-queue latency tracking
  # Maps task_id -> dequeue timestamp (for computing processing time)
  # Maps task_id -> queue_name (for attributing latency to correct queue)
  # Per-queue latency samples in milliseconds
  # Lifetime counters
  # ── Recording Events ──────────────────────────────────────────────
  # Compute latency if we tracked the dequeue time

--- File: broker/queue_manager.py ---
IMPORTS (Libraries used):
import asyncio
import uuid
import time
import logging
from collections import deque
from dataclasses import dataclass, field
from typing import Optional, Dict
from broker.dead_letter import DeadLetterQueue

CLASSES:
  class Task:: Represents a single unit of work in the queue.  Attributes: task_id:         Unique identifier (UUID4 string). queue_name:      Which named queue this task belongs to. payload:         The actual task data (arbitrary dict from the producer). status:          One of "pending", "in_flight", "done", "failed", "dead_lettered". created_at:      Unix timestamp when the task was enqueued. assigned_worker: ID of the worker currently processing this task (if any). attempts:        How many times this task has been attempted. dead_lettered_at: Unix timestamp when moved to DLQ (if applicable).
  class QueueManager:: Manages multiple named in-memory FIFO queues.  The QueueManager is the heart of the broker's data plane. It handles: - Creating queues on-the-fly when a producer first writes to them - Enqueuing tasks (PRODUCE command) - Dequeuing tasks and assigning them to a specific worker (CONSUME command) - Acknowledging successful completion (ACK command) - Handling task failure (NACK command) with retry counting: * If attempts < max_retries: re-enqueue at front of queue * If attempts >= max_retries: route to Dead Letter Queue (DLQ)  Why asyncio.Lock? asyncio coroutines are cooperative — they yield control at every `await`. Without a lock, two coroutines could interleave their reads and writes to the same queue, causing duplicated or lost tasks. The lock guarantees atomicity of each queue operation.

FUNCTIONS/METHODS:
  def __init__(self, max_retries: int = 3) -- Initialize the QueueManager.  Args: max_retries: Maximum number of attempts before a task is
  def _ensure_queue(self, queue_name: str) -- Create the named queue if it doesn't already exist.  This is called inside the lock, so it's safe from races. Using a helper keeps the enqueue/dequeue methods clean.
  async def enqueue(self, queue_name: str, payload: dict) -> str -- Add a new task to the specified queue.  Creates the queue automatically if it doesn't exist yet. Returns the unique task_id assigned to this task.

KEY CODE COMMENTS:
  # Pending queues: queue_name -> deque of Task objects waiting to be consumed
  # In-flight registry: task_id -> Task object currently being processed
  # Completed archive: task_id -> Task (for inspection and debugging)
  # Dead Letter Queue for permanently failed tasks
  # Maximum retry attempts before DLQ routing
  # Asyncio lock for safe concurrent access within the event loop

--- File: broker/server.py ---
IMPORTS (Libraries used):
import asyncio
import json
import signal
import logging
import sys
from broker.queue_manager import QueueManager, Task
from broker.worker_registry import WorkerRegistry
from broker.load_shedder import AdaptiveLoadShedder
from broker.metrics_collector import MetricsCollector
from broker.http_api import HTTPAPIServer
from persistence.wal_store import WALStore

CLASSES:
  class BrokerServer:: Asyncio-based TCP server that acts as the DistroSync message broker.  Lifecycle: 1. Create an instance: broker = BrokerServer(host, port) 2. Call await broker.start() to begin accepting connections 3. The server runs until interrupted (Ctrl+C) or shut down  The server delegates data management to: - QueueManager: handles task enqueue/dequeue/ACK/NACK - WorkerRegistry: tracks which workers are connected and alive

FUNCTIONS/METHODS:
  def __init__(self, host: str = "0.0.0.0", port: int = 5555,
  async def handle_connection(

KEY CODE COMMENTS:
  # Configure logging to show timestamps, level, and module name
  # Optional WAL-mode persistence layer
  # HTTP API server for the dashboard (initialized in start())
  # Step 1: Read the 4-byte length prefix
  # readexactly() blocks (yields) until exactly 4 bytes are available.
  # If the client disconnects mid-read, it raises IncompleteReadError.
  # Sanity check: reject absurdly large messages (> 10 MB)

--- File: broker/worker_registry.py ---
IMPORTS (Libraries used):
import asyncio
import time
import logging
from collections import deque
from dataclasses import dataclass, field
from typing import Dict, Set, Optional

CLASSES:
  class WorkerInfo:: Metadata about a single connected worker.  Attributes: worker_id:        Unique identifier for this worker. queues:           List of queue names this worker consumes from. address:          Tuple of (host, port) the worker connected from. registered_at:    Unix timestamp when the worker first connected. last_heartbeat:   Unix timestamp of the most recent heartbeat. in_flight_tasks:  Set of task_ids currently assigned to this worker. If the worker dies, these tasks get reassigned. task_start_times: Maps task_id -> monotonic timestamp when it was assigned. Used to measure task completion latency. status:           One of "active", "evicted".
  class WorkerRegistry:: Registry that tracks all known workers and evicts dead ones.  The registry has two responsibilities: 1. Tracking: know which workers are alive and what they're doing 2. Eviction: detect dead workers and reassign their tasks  The eviction loop runs as a background coroutine on the broker's event loop, checking every 2 seconds for workers whose last heartbeat is older than HEARTBEAT_TIMEOUT_SECONDS.  Why does the registry need a reference to the QueueManager? When a dead worker is evicted, its in-flight tasks need to go back into the queue. The registry holds the worker's task_ids but the QueueManager holds the actual Task objects. The registry calls queue_manager.requeue_by_id() for each orphaned task.

FUNCTIONS/METHODS:
  def __init__(self, queue_manager=None) -- Initialize the worker registry.  Args: queue_manager: Reference to the broker's QueueManager.
  async def register(

KEY CONSTANTS/CONFIG:
  HEARTBEAT_TIMEOUT_SECONDS = 6

KEY CODE COMMENTS:
  # A worker must send a heartbeat at least every 2 seconds.
  # If we miss 3 consecutive heartbeats, the worker is considered dead.
  # 3 * 2s = 6s timeout. This is the same strategy used by Redis Sentinel.
  # worker_id -> WorkerInfo
  # Reference to the queue manager for task requeuing on eviction
  # Asyncio lock — protects concurrent access to the worker dict.
  # The eviction loop and heartbeat handlers both mutate _workers.
  # Eviction loop control
  # Per-queue latency tracking (sliding window of recent latencies in ms)
  # Used by the AdaptiveLoadShedder to gauge worker performance.

--- File: persistence/wal_store.py ---
IMPORTS (Libraries used):
import sqlite3
import json
import time
import threading
import logging
from typing import Optional, List, Dict

CLASSES:
  class WALStore:: SQLite WAL-mode persistence backend for the DistroSync broker.  This class handles all database interactions. It's designed to be used by the QueueManager and DeadLetterQueue as an optional persistence layer — the broker can run without it (in-memory only) or with it (crash-safe).  Each thread gets its own SQLite connection via threading.local(), preventing the "ProgrammingError: SQLite objects created in a thread can only be used in that same thread" issue.  Usage: store = WALStore("distrosync.db") store.save_task(task)                    # Persist a task tasks = store.load_pending_tasks()       # Recover after crash store.update_task_status(task_id, "done") # Mark complete store.add_to_dlq(task, "handler crashed") # Move to DLQ dlq = store.get_dlq_tasks("emails")      # Query the DLQ task = store.replay_dlq_task(task_id)     # Replay a DLQ task

FUNCTIONS/METHODS:
  def __init__(self, db_path: str = DB_PATH) -- Initialize the WAL store.  Args: db_path: Path to the SQLite database file. The file and its
  def _get_conn(self) -> sqlite3.Connection -- Get or create a thread-local SQLite connection.  Each thread gets its own connection to avoid SQLite's thread-safety constraints. The connection is configured with:
  def _init_db(self) -- Create the database schema if it doesn't exist.  This is idempotent — safe to call on every startup. The schema uses IF NOT EXISTS so it won't clobber existing data.

KEY CONSTANTS/CONFIG:
  DB_PATH = "distrosync.db"============================================================
=== REPO: Transformer_from_scratch_using_pytorch ===
URL: https://github.com/udayraj1238/Transformer_from_scratch_using_pytorch
Description: Complete Attention Is All You Need transformer in pure PyTorch — MHSA, positional encoding, encoder-decoder, beam search.
Primary Language: Jupyter Notebook | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: Transformer_from_scratch_using_pytorch/model.py ---
IMPORTS (Libraries used):
import torch
import torch.nn as nn
import math

CLASSES:
  class InputEmbeddings(nn.Module):
  class PositionalEncoding(nn.Module):
  class LayerNormalization(nn.Module):
  class FeedForwardBlock(nn.Module):
  class MultiHeadAttentionBlock(nn.Module):
  class ResidualConnection(nn.Module):
  class EncoderBlock(nn.Module):

FUNCTIONS/METHODS:
  def __init__(self , d_model: int , vocab_size: int)
  def forward(self , x)
  def __init__(self , d_model: int , seq_len: int, dropout: float) -> None
  def forward(self , x)
  def __init__(self , eps : float = 10**-6) -> None
  def forward(self , x)
  def __init__(self , d_model : int , d_ff : int , dropout : float) -> None
  def forward(self , x)
  def __init__(self, d_model: int, h: int, dropout: float) -> None
  def attention(query, key, value, mask, dropout: nn.Dropout)
  def forward(self, q, k, v, mask)
  def __init__(self, features: int, dropout: float) -> None
  def forward(self, x, sublayer)
  def __init__(self, features: int, self_attention_block: MultiHeadAttentionBlock, feed_forward_block: FeedForwardBlock, dropout: float) -> None
  def forward(self, x, src_mask)

KEY CODE COMMENTS:
  # create a matrix of shape (seq_len , d_model)
  # create a vector of length (seq_len)
  # Apply the sin to even positions
  # (Batch , Seq_len , d_model) ---> (Batch , Seq_len , d_ff) ---> (batch , seq_len , d_model)
  # Make sure d_model is divisible by h
  # Just apply the formula from the paper
  # (batch, h, seq_len, d_k) --> (batch, h, seq_len, seq_len)
  # Write a very low value (indicating -inf) to the positions where mask == 0
  # (batch, h, seq_len, seq_len) --> (batch, h, seq_len, d_k)
  # return attention scores which can be used for visualization

--- File: Transformer_from_scratch_using_pytorch/train.py ---
IMPORTS (Libraries used):
from model import build_transformer
from dataset import BilingualDataset, causal_mask
from config import get_config, get_weights_file_path, latest_weights_file_path
import torch
import torch.nn as nn
from torch.utils.data import Dataset, DataLoader, random_split
from torch.optim.lr_scheduler import LambdaLR
import warnings
from tqdm import tqdm
import os
from pathlib import Path
from datasets import load_dataset
from tokenizers import Tokenizer
from tokenizers.models import WordLevel
from tokenizers.trainers import WordLevelTrainer
from tokenizers.pre_tokenizers import Whitespace
import torchmetrics
from torch.utils.tensorboard import SummaryWriter

FUNCTIONS/METHODS:
  def greedy_decode(model, source, source_mask, tokenizer_src, tokenizer_tgt, max_len, device)
  def run_validation(model, validation_ds, tokenizer_src, tokenizer_tgt, max_len, device, print_msg, global_step, writer, num_examples=2)
  def get_all_sentences(ds, lang)
  def get_or_build_tokenizer(config, ds, lang)
  def get_ds(config)

KEY CODE COMMENTS:
  # Huggingface datasets and tokenizers
  # Precompute the encoder output and reuse it for every step
  # Initialize the decoder input with the sos token
  # build mask for target
  # calculate output
  # get the console window width
  # If we can't get the console width, use 80 as default
  # check that the batch size is 1
  # Print the source, target and model output
  # Evaluate the character error rate

--- File: README.md ---
<div align="center">

<!-- Animated Header -->
<img src="https://capsule-render.vercel.app/api?type=waving&color=0:F97316,50:EF4444,100:DC2626&height=220&section=header&text=Transformer&fontSize=50&fontColor=ffffff&animation=fadeIn&fontAlignY=35&desc=Built%20from%20Scratch%20in%20PyTorch&descSize=18&descAlignY=55&descAlign=50" width="100%"/>

<!-- Typing SVG -->
<a href="https://git.io/typing-svg"><img src="https://readme-typing-svg.demolab.com?font=Fira+Code&weight=600&size=22&pause=1000&color=F97316&center=true&vCenter=true&width=700&lines=Attention+Is+All+You+Need+(2017);Multi-Head+Self-Attention;Positional+Encoding+%7C+Encoder-Decoder;No+nn.Transformer+%E2%80%94+Pure+PyTorch" alt="Typing SVG" /></a>

<br/>

<!-- Badges -->
<img src="https://img.shields.io/badge/PyTorch-EE4C2C?style=for-the-badge&logo=pytorch&logoColor=white" />
<img src="https://img.shields.io/badge/Python-3776AB?style=for-the-badge&logo=python&logoColor=white" />

<br/><br/>

<!-- Paper Badge -->
<a href="https://arxiv.org/abs/1706.03762"><img src="https://img.shields.io/badge/arXiv-1706.03762-b31b1b?style=flat-square&logo=arxiv&logoColor=white" /></a>
<img src="https://img.shields.io/badge/Vaswani%20et%20al.-NeurIPS%202017-F97316?style=flat-square&labelColor=1a1a2e" />

</div>

---

<div align="center">
<h2>🎯 What This Is</h2>
<p><i>A complete from-scratch PyTorch implementation of the original Transformer architecture.<br/>Every component — multi-head attention, positional encoding, encoder, decoder — written by hand without <code>nn.Transformer</code>.</i></p>
</div>

---

<div align="center">
<h2>🏗️ Architecture</h2>
</div>

```mermaid
graph TD
    subgraph Encoder["🔵 ENCODER (N layers)"]
        A["Source Tokens"] --> AE["Embedding + Positional Encoding"]
        AE --> SA["Multi-Head Self-Attention"]
        SA --> AN1["Add & Norm"]
        AN1 --> FF1["Feed-Forward Network"]
        FF1 --> AN2["Add & Norm"]
    end
    
    subgraph Decoder["🔴 DECODER (N layers)"]
        B["Target Tokens"] --> BE["Embedding + Positional Encoding"]
        BE --> MSA["Masked Self-Attention"]
        MSA --> AN3["Add & Norm"]
        AN3 --> CA["Cross-Attention"]
        AN2 -->|"K, V"| CA
        CA --> AN4["Add & Norm"]
        AN4 --> FF2["Feed-Forward Network"]
        FF2 --> AN5["Add & Norm"]
    end
    
    AN5 --> LIN["Linear + Softmax"]
    LIN --> OUT["📤 Output Tokens"]
    
    style A fill:#3B82F6,stroke:#3B82F6,color:#fff
    style AE fill:#60A5FA,stroke:#60A5FA,color:#fff
    style SA fill:#2563EB,stroke:#2563EB,color:#fff
    style AN1 fill:#1D4ED8,stroke:#1D4ED8,color:#fff
    style FF1 fill:#1E40AF,stroke:#1E40AF,color:#fff
    style AN2 fill:#1D4ED8,stroke:#1D4ED8,color:#fff
    style B fill:#EF4444,stroke:#EF4444,color:#fff
    style BE fill:#F87171,stroke:#F87171,color:#fff
    style MSA fill:#DC2626,stroke:#DC2626,color:#fff
    style AN3 fill:#B91C1C,stroke:#B91C1C,color:#fff
    style CA fill:#F97316,stroke:#F97316,color:#fff
    style AN4 fill:#B91C1C,stroke:#

--- File: Transformer_from_scratch_using_pytorch/config.py ---
IMPORTS (Libraries used):
from pathlib import Path

FUNCTIONS/METHODS:
  def get_config()
  def get_weights_file_path(config, epoch: str)
  def latest_weights_file_path(config)

KEY CODE COMMENTS:
  # Find the latest weights file in the weights folder

--- File: Transformer_from_scratch_using_pytorch/dataset.py ---
IMPORTS (Libraries used):
import torch
import torch.nn as nn
from torch.utils.data import Dataset

CLASSES:
  class BilingualDataset(Dataset):

FUNCTIONS/METHODS:
  def __init__(self, ds, tokenizer_src, tokenizer_tgt, src_lang, tgt_lang, seq_len)
  def __len__(self)
  def __getitem__(self, idx)
  def causal_mask(size)

KEY CODE COMMENTS:
  # Transform the text into tokens
  # Add sos, eos and padding to each sentence
  # We will only add <s>, and </s> only on the label
  # Make sure the number of padding tokens is not negative. If it is, the sentence is too long
  # Add <s> and </s> token
  # Add only <s> token
  # Add only </s> token
  # Double check the size of the tensors to make sure they are all seq_len long============================================================
=== REPO: Pytorch_PaliGemma ===
URL: https://github.com/udayraj1238/Pytorch_PaliGemma
Description: PaliGemma VLM from scratch in PyTorch — SigLIP vision encoder + Gemma-2B decoder + 8-bit quantization. 22% BLEU-4 improvement.
Primary Language: Python | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: README.md ---
<div align="center">

<!-- Animated Header -->
<img src="https://capsule-render.vercel.app/api?type=waving&color=0:7C3AED,50:8B5CF6,100:A78BFA&height=220&section=header&text=PaliGemma%20VLM&fontSize=50&fontColor=ffffff&animation=fadeIn&fontAlignY=35&desc=Vision-Language%20Model%20from%20Scratch&descSize=18&descAlignY=55&descAlign=50" width="100%"/>

<!-- Typing SVG -->
<a href="https://git.io/typing-svg"><img src="https://readme-typing-svg.demolab.com?font=Fira+Code&weight=600&size=22&pause=1000&color=A78BFA&center=true&vCenter=true&width=700&lines=No+Hugging+Face.+No+Pre-built+Modules.;Every+Tensor%2C+Every+Layer%2C+Every+Attention+Head;SigLIP+%2B+Gemma+%2B+Multimodal+Projector;Built+From+the+Ground+Up+in+PyTorch" alt="Typing SVG" /></a>

<br/>

<!-- Tech Badges -->
<img src="https://img.shields.io/badge/PyTorch-EE4C2C?style=for-the-badge&logo=pytorch&logoColor=white" />
<img src="https://img.shields.io/badge/Python-3776AB?style=for-the-badge&logo=python&logoColor=white" />
<img src="https://img.shields.io/badge/CUDA-76B900?style=for-the-badge&logo=nvidia&logoColor=white" />

<br/><br/>

<!-- Stats -->
<img src="https://img.shields.io/badge/12%20Layer-Vision%20Transformer-7C3AED?style=flat-square&labelColor=1a1a2e" />
<img src="https://img.shields.io/badge/196-Visual%20Tokens-8B5CF6?style=flat-square&labelColor=1a1a2e" />
<img src="https://img.shields.io/badge/8192-Token%20Context-A78BFA?style=flat-square&labelColor=1a1a2e" />
<img src="https://img.shields.io/badge/768→2048-Projection-C4B5FD?style=flat-square&labelColor=1a1a2e" />

</div>

---

<div align="center">
<h2>🎯 What This Is</h2>
<p><i>A complete from-scratch PyTorch implementation of Google's PaliGemma Vision-Language Model.</i></p>
</div>

> **Why from scratch?** Because calling `model = AutoModel.from_pretrained()` teaches you nothing about *how* the model actually processes images and generates text. Building every component by hand — from patch embeddings to KV-cache — is the only way to truly understand modern VLMs.

---

<div align="center">
<h2>🏗️ Architecture</h2>
</div>

```mermaid
graph TD
    A["🖼️ Input Image"] --> B["SigLIP Vision Encoder"]
    T["📝 Text Prompt"] --> C["Text Tokenizer"]
    
    subgraph Vision["🔵 SigLIP Vision Transformer"]
        B --> B1["16×16 Patch Embedding"]
        B1 --> B2["12 Transformer Layers"]
        B2 --> B3["196 Visual Tokens (768-dim)"]
    end
    
    subgraph Proj["🟣 Multimodal Bridge"]
        B3 --> P1["Linear Projection"]
        P1 --> P2["768 → 2048 dim"]
    end
    
    C --> C1["Token Embeddings"]
    
    subgraph LLM["🔴 Gemma Language Decoder"]
        P2 --> D1["Merged Sequence @ idx 256000"]
        C1 --> D1
        D1 --> D2["RMSNorm + RoPE"]
        D2 --> D3["Grouped KV Attention"]
        D3 --> D4["KV-Cache"]
        D4 --> D5["Causal Masking"]
        D5 --> D6["8192-token Context"]
    end
    
    D6 --> OUT["💬 Generated Text"]
    
    style A fill:#3B82F6,stroke:#3B82F6,color:#fff
    style T fill:#F59E0B,strok

--- File: modeling_siglip.py ---
IMPORTS (Libraries used):
from typing import Optional, Tuple
import torch
import torch.nn as nn

CLASSES:
  class SiglipVisionConfig:
  class SiglipVisionEmbeddings(nn.Module):
  class SiglipAttention(nn.Module):: Multi-headed attention from 'Attention Is All You Need' paper

FUNCTIONS/METHODS:
  def __init__(
  def __init__(self, config: SiglipVisionConfig)
  def forward(self, pixel_values: torch.FloatTensor) -> torch.Tensor
  def __init__(self, config)
  def forward(

KEY CODE COMMENTS:
  # Convolve the `patch_size` kernel over the image, with no overlapping patches since the stride is equal to the kernel size
  # The output of the convolution will have shape [Batch_Size, Embed_Dim, Num_Patches_H, Num_Patches_W]
  # where Num_Patches_H = height // patch_size and Num_Patches_W = width // patch_size
  # [Batch_Size, Embed_Dim, Num_Patches_H, Num_Patches_W] -> [Batch_Size, Embed_Dim, Num_Patches]
  # where Num_Patches = Num_Patches_H * Num_Patches_W
  # [Batch_Size, Embed_Dim, Num_Patches] -> [Batch_Size, Num_Patches, Embed_Dim]
  # Add position embeddings to each patch. Each positional encoding is a vector of size [Embed_Dim]
  # [Batch_Size, Num_Patches, Embed_Dim]
  # hidden_states: [Batch_Size, Num_Patches, Embed_Dim]
  # query_states: [Batch_Size, Num_Patches, Embed_Dim]

--- File: modelling_gemma.py ---
IMPORTS (Libraries used):
import torch
from torch import nn
from typing import Optional, Tuple, List
from torch.nn import CrossEntropyLoss
import math
from modeling_siglip import SiglipVisionConfig, SiglipVisionModel

CLASSES:
  class KVCache():
  class GemmaConfig():
  class PaliGemmaConfig():
  class GemmaRMSNorm(nn.Module):
  class GemmaRotaryEmbedding(nn.Module):

FUNCTIONS/METHODS:
  def __init__(self) -> None
  def num_items(self) -> int
  def update(
  def __init__(
  def __init__(
  def __init__(self, dim: int, eps: float = 1e-6)
  def _norm(self, x)
  def forward(self, x)
  def __init__(self, dim, max_position_embeddings=2048, base=10000, device=None)
  def forward(self, x, position_ids, seq_len=None)

KEY CODE COMMENTS:
  # The shape of the key_cache is [Batch_Size, Num_Heads_KV, Seq_Len, Head_Dim]
  # If we never added anything to the KV-Cache of this layer, let's create it.
  # ... otherwise we concatenate the new keys with the existing ones.
  # each tensor has shape: [Batch_Size, Num_Heads_KV, Seq_Len, Head_Dim]
  # ... and then we return all the existing keys + the new ones.
  # Llama does x.to(float16) * w whilst Gemma is (x * w).to(float16)
  # See https://github.com/huggingface/transformers/pull/29402
  # Calculate the theta according to the formula theta_i = base^(-2i/dim) where i = 0, 1, 2, ..., dim // 2
  # x: [bs, num_attention_heads, seq_len, head_size]
  # Copy the inv_freq tensor for batch in the sequence

--- File: processing_paligemma.py ---
IMPORTS (Libraries used):
from typing import Dict, List, Optional, Union, Tuple, Iterable
import numpy as np
from PIL import Image
import torch

CLASSES:
  class PaliGemmaProcessor:

FUNCTIONS/METHODS:
  def add_image_tokens_to_prompt(prefix_prompt, bos_token, image_seq_len, image_token)
  def rescale(
  def resize(
  def normalize(
  def process_images(
  def __init__(self, tokenizer, num_image_tokens: int, image_size: int)
  def __call__(

KEY CONSTANTS/CONFIG:
  IMAGENET_STANDARD_MEAN = [0.5, 0.5, 0.5]
  IMAGENET_STANDARD_STD = [0.5, 0.5, 0.5]
  IMAGE_TOKEN = "<image>"
  EXTRA_TOKENS = [

KEY CODE COMMENTS:
  # Quoting from the blog (https://huggingface.co/blog/paligemma#detailed-inference-process):
  # The input text is tokenized normally.
  # A <bos> token is added at the beginning, and an additional newline token (\n) is appended.
  # This newline token is an essential part of the input prompt the model was trained with, so adding it explicitly ensures it's always there.
  # The tokenized text is also prefixed with a fixed number of <image> tokens.
  # NOTE: from the paper it looks like the `\n` should be tokenized separately, but in the HF implementation this is not done.
  # ref to HF implementation: https://github.com/huggingface/transformers/blob/7f79a97399bb52aad8460e1da2f36577d5dccfed/src/transformers/models/paligemma/processing_paligemma.py#L55-L73
  # Convert each image to a numpy array
  # Rescale the pixel values to be in the range [0, 1]
  # Normalize the images to have mean 0 and standard deviation 1============================================================
=== REPO: CourtSense-AI ===
URL: https://github.com/udayraj1238/CourtSense-AI
Description: Tennis match video → interactive 3D replay. YOLOv8-Pose + SegFormer-B2 + Kalman filter + FastAPI + Three.js. 30+ FPS on edge devices.
Primary Language: TypeScript | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: backend/main.py ---
IMPORTS (Libraries used):
import json
import os
from contextlib import asynccontextmanager
import numpy as np
from fastapi import FastAPI, HTTPException
from fastapi.middleware.cors import CORSMiddleware
from backend.api.routes import health, jobs
from backend.config import get_settings
from backend.models.api import SequenceResponse
from backend.models.tracking import BallState, Coordinate, FrameData, PlayerState
from backend.services.job_manager import get_job_manager

FUNCTIONS/METHODS:
  async def lifespan(app: FastAPI)
  def create_app() -> FastAPI
  def get_mock_tracking_sequence()
  def get_real_tracking_sequence()

KEY CONSTANTS/CONFIG:
  REPO_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))

KEY CODE COMMENTS:
  # Legacy script path for /api/v1/tracking/real
  # --- Legacy v1 endpoints (preserved for dev / HF backward compat) ---

--- File: README.md ---
---
title: CourtSense Backend
emoji: 🎾
colorFrom: green
colorTo: blue
sdk: docker
app_port: 8000
---
<div align="center">

<!-- Animated Header -->
<img src="https://capsule-render.vercel.app/api?type=waving&color=0:0D9488,50:06B6D4,100:3B82F6&height=220&section=header&text=CourtSense%20AI&fontSize=50&fontColor=ffffff&animation=fadeIn&fontAlignY=35&desc=3D%20Tennis%20Analytics%20Engine&descSize=18&descAlignY=55&descAlign=50" width="100%"/>

<!-- Typing SVG -->
<a href="https://git.io/typing-svg"><img src="https://readme-typing-svg.demolab.com?font=Fira+Code&weight=600&size=22&pause=1000&color=06B6D4&center=true&vCenter=true&width=600&lines=Tennis+Video+%E2%86%92+Interactive+3D+Replay;YOLOv8-Pose+%7C+Hough+Lines+%7C+Three.js;Physics-Based+Synthesis+%26+Replays;150-Frame+Physics-Accurate+Replays" alt="Typing SVG" /></a>

<br/>
<br/>

<a href="https://udayraj1238.github.io/CourtSense-AI/" target="_blank">
  <img src="https://img.shields.io/badge/🎾_Play_Live_Demo-Click_Here_to_Launch-06B6D4?style=for-the-badge&logo=react&logoColor=white" height="50" alt="Launch Live Web App" />
</a>

<br/>

<!-- Badges Row 1 -->
<img src="https://img.shields.io/badge/Python-3776AB?style=for-the-badge&logo=python&logoColor=white" />
<img src="https://img.shields.io/badge/PyTorch-EE4C2C?style=for-the-badge&logo=pytorch&logoColor=white" />
<img src="https://img.shields.io/badge/FastAPI-009688?style=for-the-badge&logo=fastapi&logoColor=white" />
<img src="https://img.shields.io/badge/React-61DAFB?style=for-the-badge&logo=react&logoColor=black" />
<img src="https://img.shields.io/badge/Three.js-000000?style=for-the-badge&logo=three.js&logoColor=white" />
<img src="https://img.shields.io/badge/OpenCV-5C3EE8?style=for-the-badge&logo=opencv&logoColor=white" />

<br/>

<!-- Badges Row 2 -->
<img src="https://img.shields.io/badge/TypeScript-3178C6?style=for-the-badge&logo=typescript&logoColor=white" />
<img src="https://img.shields.io/badge/Docker-2496ED?style=for-the-badge&logo=docker&logoColor=white" />
<img src="https://img.shields.io/badge/MediaPipe-0097A7?style=for-the-badge&logo=google&logoColor=white" />

<br/><br/>

<!-- Stats Badges -->
<img src="https://img.shields.io/badge/150%20Frames-Per%20Replay-0D9488?style=flat-square&labelColor=1a1a2e" />
<img src="https://img.shields.io/badge/17%20Keypoints-Per%20Frame-06B6D4?style=flat-square&labelColor=1a1a2e" />
<img src="https://img.shields.io/badge/~70%25%20Jitter-Reduction-3B82F6?style=flat-square&labelColor=1a1a2e" />
<img src="https://img.shields.io/badge/±5%20km%2Fh-Speed%20Accuracy-8B5CF6?style=flat-square&labelColor=1a1a2e" />

</div>

---

<div align="center">
<h2>🎯 What It Does</h2>
</div>

<table>
<tr>
<td width="50%">

### 📥 Input
A raw tennis match video from **any camera angle** — broadcast footage, phone recording, or drone shot.

</td>
<td width="50%">

### 📤 Output
An **interactive 3D replay** you can rotate, zoom, and replay from any angle — with real-time **speed (km/h)** and **spin (rpm)** overlays.

</

--- File: frontend/README.md ---
# React + TypeScript + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) (or [oxc](https://oxc.rs) when used in [rolldown-vite](https://vite.dev/guide/rolldown)) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend updating the configuration to enable type-aware lint rules:

```js
export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...

      // Remove tseslint.configs.recommended and replace with this
      tseslint.configs.recommendedTypeChecked,
      // Alternatively, use this for stricter rules
      tseslint.configs.strictTypeChecked,
      // Optionally, add this for stylistic rules
      tseslint.configs.stylisticTypeChecked,

      // Other configs...
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```

You can also install [eslint-plugin-react-x](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x) and [eslint-plugin-react-dom](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom) for React-specific lint rules:

```js
// eslint.config.js
import reactX from 'eslint-plugin-react-x'
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```

--- File: backend/config.py ---
IMPORTS (Libraries used):
from functools import lru_cache
from pathlib import Path
from pydantic_settings import BaseSettings, SettingsConfigDict

CLASSES:
  class Settings(BaseSettings):

FUNCTIONS/METHODS:
  def get_settings() -> Settings

KEY CONSTANTS/CONFIG:
  REPO_ROOT = Path(__file__).resolve().parent.parent

KEY CODE COMMENTS:
  # Stub pipeline simulates CV work (seconds). Set 0 for instant response in tests.

--- File: backend/cv/ball_tracker.py ---
IMPORTS (Libraries used):
import cv2
import numpy as np
from typing import Dict, Any, Optional
from ultralytics import YOLO
from backend.cv.homography import CourtProjector

CLASSES:
  class BallTracker:

FUNCTIONS/METHODS:
  def __init__(self, model_path: str = "yolov8_tennis_ball.pt")
  def detect_ball(self, frame: np.ndarray, projector: CourtProjector) -> Optional[Dict[str, float]] -- Detects the tennis ball in the frame and projects it to the 2D court ground plane (x, z). Note: The true height (y) is estimated in the pipeline stage, not here. Returns: {"x": float, "z": float, "conf": float, "u": float, "v": float} or None

KEY CONSTANTS/CONFIG:
  INITIAL_CONF = 0.15
  FINAL_CONF = 0.60

KEY CODE COMMENTS:
  # We use a custom fine-tuned YOLOv8 model for V5.0.
  # In our custom dataset, class 0 is 'tennis_ball'.
  # Fallback to standard COCO
  # Tennis-Vision dual-threshold pipeline variables
  # Get center of bounding box
  # Project pixel to court ground plane (x, z)
  # Court Bounds Filter: A ball can be out of bounds, but usually within a reasonable distance
  # Sort by confidence and return the best one

--- File: backend/cv/court_lines.py ---
IMPORTS (Libraries used):
from __future__ import annotations
import cv2
import numpy as np
from backend.cv.errors import CalibrationError

FUNCTIONS/METHODS:
  def _resize(frame: np.ndarray, max_width: int = 1280) -> tuple[np.ndarray, float]
  def _white_line_mask(bgr: np.ndarray) -> np.ndarray
  def _line_angle(x1: float, y1: float, x2: float, y2: float) -> float
  def _collect_segments(mask: np.ndarray) -> list[tuple[float, float, float, float]]
  def _cluster_lines(
  def _line_to_abc(x1: float, y1: float, x2: float, y2: float) -> tuple[float, float, float]
  def _intersect(l1: tuple[float, float, float], l2: tuple[float, float, float]) -> np.ndarray | None
  def _order_corners(pts: np.ndarray) -> np.ndarray -- Order 4 points as TL, TR, BR, BL.
  def _pick_extreme_lines(
  def detect_court_corners(frame: np.ndarray) -> np.ndarray -- Detect 4 court corners in pixel space (TL, TR, BR, BL). Raises CalibrationError if detection fails validation.  TODO: Replace Hough lines with Tennis-Vision's keypoints_model.pth (ResNet-50)
  def extract_preview_frame(video_path: str, out_jpg: str, timestamp_sec: float = 0.5) -> np.ndarray -- Grab a BGR

KEY CODE COMMENTS:
  # Adaptive threshold — works on all court types
  # White/light color mask — supplement for hard courts
  # Also detect yellow lines (some indoor courts)
  # top = min y-midpoint, bottom = max y-midpoint

--- File: backend/cv/errors.py ---
CLASSES:
  class CalibrationError(Exception):: Auto or manual court calibration failed.

--- File: backend/cv/homography.py ---
IMPORTS (Libraries used):
from __future__ import annotations
import json
import cv2
import numpy as np
from backend.cv.errors import CalibrationError
from backend.cv.court_lines import detect_court_corners

CLASSES:
  class CourtProjector:

FUNCTIONS/METHODS:
  def __init__(self, homography: np.ndarray)
  def from_corners(cls, src_corners: np.ndarray) -> "CourtProjector" -- Build projector from 4 image corners (TL, TR, BR, BL).
  def from_frame(cls, frame: np.ndarray) -> "CourtProjector"
  def pixel_to_court(self, u: float, v: float) -> tuple[float, float] -- Returns (x, z) in meters on the ground plane.
  def pixel_to_3d(self, u: float, v: float, y: float = 0.0) -> tuple[float, float, float]
  def clamp_to_court(self, x: float, z: float) -> tuple[float, float]
  def reprojection_error_px(self, src_corners: np.ndarray) -> float
  def to_json(self) -> str
  def from_json(cls, data: str) -> "CourtProjector"

KEY CONSTANTS/CONFIG:
  COURT_DST = np.array(

KEY CODE COMMENTS:
  # Destination court corners in meters (x, z): TL, TR, BR, BL

--- File: backend/cv/kalman3d.py ---
IMPORTS (Libraries used):
import cv2
import numpy as np

CLASSES:
  class Kalman3D:

FUNCTIONS/METHODS:
  def __init__(self, dt: float = 1/30.0)
  def init_state(self, x: float, y: float, z: float)
  def predict(self) -> tuple[float, float, float] -- Predict next state and return predicted (x, y, z).
  def correct(self, x: float, y: float, z: float) -> tuple[float, float, float] -- Update with measurement and return corrected (x, y, z).
  def get_velocity(self) -> tuple[float, float, float]

KEY CONSTANTS/CONFIG:
  GRAVITY = 9.81

KEY CODE COMMENTS:
  # State vector: [x, y, z, vx, vy, vz]
  # Measurement vector: [x, y, z]
  # Control vector: [1] (for gravity)
  # Transition matrix (A)
  # x_t = x_{t-1} + vx_{t-1} * dt
  # Control matrix (B)
  # Measurement matrix (H)
  # We only measure x, y, z
  # Process noise covariance (Q)
  # Small noise for position, larger for velocity to allow for sudden changes (hits/bounces)============================================================
=== REPO: udayraj1238 ===
URL: https://github.com/udayraj1238/udayraj1238
Description: My GitHub profile
Primary Language: Unknown | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: README.md ---
<div align="center">

<!-- Animated Header -->
<img src="https://capsule-render.vercel.app/api?type=waving&color=0:0D9488,25:06B6D4,50:3B82F6,75:8B5CF6,100:EC4899&height=220&section=header&text=Uday%20Raj&fontSize=60&fontColor=ffffff&animation=fadeIn&fontAlignY=35&desc=AI%20%26%20Computer%20Vision%20Engineer&descSize=20&descAlignY=55&descAlign=50" width="100%"/>

<!-- Typing SVG -->
<a href="https://git.io/typing-svg"><img src="https://readme-typing-svg.demolab.com?font=Fira+Code&weight=600&size=24&pause=1000&color=06B6D4&center=true&vCenter=true&width=800&lines=Building+intelligent+systems+from+research+to+production;Research+Intern+%40+SVNIT+Surat;From-scratch+VLMs+%7C+3D+Sports+Analytics+%7C+Adversarial+ML;B.Tech+AI+%26+Data+Science+%40+IIITDM+Kurnool" alt="Typing SVG" /></a>

<br/>

<!-- Social Badges -->
<a href="https://www.linkedin.com/in/uday6002/"><img src="https://img.shields.io/badge/LinkedIn-0A66C2?style=for-the-badge&logo=linkedin&logoColor=white" /></a>
<a href="https://udayraj1238.vercel.app"><img src="https://img.shields.io/badge/Portfolio-000000?style=for-the-badge&logo=vercel&logoColor=white" /></a>
<a href="mailto:rajuday6002@gmail.com"><img src="https://img.shields.io/badge/Email-EA4335?style=for-the-badge&logo=gmail&logoColor=white" /></a>

<br/>

<!-- Visitor Counter -->
<img src="https://komarev.com/ghpvc/?username=udayraj1238&style=flat-square&color=0D9488&label=Profile+Views" />

</div>

---

<div align="center">
<h2>🔬 What I'm Working On</h2>
</div>

<table>
<tr>
<td align="center" width="33%">
<img src="https://img.shields.io/badge/🔬-Research_Intern-0D9488?style=for-the-badge&labelColor=1a1a2e" /><br/><br/>
<b>SVNIT Surat</b><br/>
Adversarial robustness research on transformer-based CV systems.<br/>Paper under review.
</td>
<td align="center" width="33%">
<img src="https://img.shields.io/badge/🤖-ML_Coordinator-3B82F6?style=for-the-badge&labelColor=1a1a2e" /><br/><br/>
<b>Google Developer Groups</b><br/>
Led 12-week ML bootcamp for 200+ students.
</td>
<td align="center" width="33%">
<img src="https://img.shields.io/badge/👁️-CV_Coordinator-8B5CF6?style=for-the-badge&labelColor=1a1a2e" /><br/><br/>
<b>DataWorks Club</b><br/>
Mentoring 100+ members on real-time CV pipelines.
</td>
</tr>
</table>

---

<div align="center">
<h2>🚀 Featured Projects</h2>
</div>

<table>
<tr>
<td width="50%">

### [🎾 CourtSense AI](https://github.com/udayraj1238/CourtSense-AI)
**3D Tennis Analytics Engine**

Converts tennis videos into interactive 3D replays with real-time speed & spin stats.

<img src="https://img.shields.io/badge/YOLOv8-FF6F00?style=flat-square" /> <img src="https://img.shields.io/badge/SegFormer-5C3EE8?style=flat-square" /> <img src="https://img.shields.io/badge/Three.js-000?style=flat-square" /> <img src="https://img.shields.io/badge/FastAPI-009688?style=flat-square" /> <img src="https://img.shields.io/badge/React-61DAFB?style=flat-square" />

</td>
<td width="50%">

### [🧠 PaliGemma VLM](https://github.com/udayraj1238/Pyto============================================================
=== REPO: grid07-cognitive-engine ===
URL: https://github.com/udayraj1238/grid07-cognitive-engine
Description: AI Cognitive Engine: Vector Routing, LangGraph Orchestration, RAG Combat Engine with Prompt Injection Defense
Primary Language: Python | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: main.py ---
IMPORTS (Libraries used):
import sys
import time
from rich.panel import Panel
from rich import box
from utils.logging_config import console, print_success, print_error, print_info
from phase1_router import run_phase1_demo
from phase2_content_engine import run_phase2_demo
from phase3_combat_engine import run_phase3_demo

FUNCTIONS/METHODS:
  def print_banner() -- Print the Grid07 Cognitive Engine banner.
  def main() -- Main entry point — runs selected or all phases.

KEY CODE COMMENTS:
  # Parse simple CLI flags
  # ─── Phase 1: Vector-Based Persona Matching ─────────────
  # ─── Phase 2: Autonomous Content Engine ─────────────────
  # ─── Phase 3: Combat Engine ─────────────────────────────
  # ─── Summary ────────────────────────────────────────────

--- File: README.md ---
<div align="center">

<!-- Animated Header -->
<img src="https://capsule-render.vercel.app/api?type=waving&color=0:06B6D4,50:0EA5E9,100:3B82F6&height=220&section=header&text=Grid07%20Cognitive%20Engine&fontSize=42&fontColor=ffffff&animation=fadeIn&fontAlignY=35&desc=AI%20Routing%20%7C%20LangGraph%20%7C%20RAG%20%7C%20Prompt%20Defense&descSize=16&descAlignY=55&descAlign=50" width="100%"/>

<!-- Typing SVG -->
<a href="https://git.io/typing-svg"><img src="https://readme-typing-svg.demolab.com?font=Fira+Code&weight=600&size=22&pause=1000&color=0EA5E9&center=true&vCenter=true&width=650&lines=Vector+Semantic+Query+Routing;Multi-Agent+LangGraph+Orchestration;RAG+Combat+Engine;Multi-Layered+Prompt+Injection+Defense" alt="Typing SVG" /></a>

<br/>

<!-- Badges -->
<img src="https://img.shields.io/badge/Python-3776AB?style=for-the-badge&logo=python&logoColor=white" />
<img src="https://img.shields.io/badge/LangChain-1C3C3C?style=for-the-badge&logo=langchain&logoColor=white" />
<img src="https://img.shields.io/badge/LangGraph-FF6F00?style=for-the-badge&logo=graphql&logoColor=white" />
<img src="https://img.shields.io/badge/RAG-8B5CF6?style=for-the-badge&logo=elasticsearch&logoColor=white" />

</div>

---

<div align="center">
<h2>🎯 What It Does</h2>
<p><i>An intelligent AI backend that routes queries through semantic vectors, orchestrates multi-step reasoning, and retrieves context-aware responses — all secured against prompt injection attacks.</i></p>
</div>

---

<div align="center">
<h2>🏗️ Three-Phase Architecture</h2>
</div>

```mermaid
graph TD
    A["💬 User Query"] --> B["🛡️ Prompt Injection Shield"]
    
    B --> C["Phase 1: Router"]
    
    subgraph P1["🔵 Phase 1 — Semantic Router"]
        C --> C1["Vector Embedding"]
        C1 --> C2["Cosine Similarity"]
        C2 --> C3["Intent Classification"]
    end
    
    C3 --> D["Phase 2: Content Engine"]
    
    subgraph P2["🟣 Phase 2 — Content Engine"]
        D --> D1["LangGraph State Machine"]
        D1 --> D2["Multi-Agent Workflow"]
        D2 --> D3["Conditional Branching"]
    end
    
    D3 --> E["Phase 3: Combat Engine"]
    
    subgraph P3["🔴 Phase 3 — RAG Combat"]
        E --> E1["Vector DB Retrieval"]
        E1 --> E2["Context Injection"]
        E2 --> E3["Grounded Generation"]
    end
    
    E3 --> F["✅ Secure Response"]
    
    style A fill:#F59E0B,stroke:#F59E0B,color:#fff
    style B fill:#EF4444,stroke:#EF4444,color:#fff
    style C fill:#3B82F6,stroke:#3B82F6,color:#fff
    style C1 fill:#60A5FA,stroke:#60A5FA,color:#fff
    style C2 fill:#60A5FA,stroke:#60A5FA,color:#fff
    style C3 fill:#60A5FA,stroke:#60A5FA,color:#fff
    style D fill:#8B5CF6,stroke:#8B5CF6,color:#fff
    style D1 fill:#A78BFA,stroke:#A78BFA,color:#fff
    style D2 fill:#A78BFA,stroke:#A78BFA,color:#fff
    style D3 fill:#A78BFA,stroke:#A78BFA,color:#fff
    style E fill:#EF4444,stroke:#EF4444,color:#fff
    style E1 fill:#F87171,stroke:#F87171,color:#fff
    style E2 fill:#F87171,stroke:#F87171,color:

--- File: config.py ---
IMPORTS (Libraries used):
import os
from dotenv import load_dotenv

KEY CONSTANTS/CONFIG:
  GROQ_API_KEY = os.getenv("GROQ_API_KEY", "")
  GROQ_MODEL = "llama-3.3-70b-versatile"       # Best free-tier model for tool-calling
  GROQ_TEMPERATURE = 0                           # Deterministic output for structured data
  GROQ_MAX_TOKENS = 1024                         # Sufficient for 280-char posts + reasoning
  VECTOR_COLLECTION_NAME = "bot_personas"
  VECTOR_DISTANCE_METRIC = "cosine"              # ChromaDB uses cosine distance
  DEFAULT_SIMILARITY_THRESHOLD = 0.15
  CANARY_TOKEN = "GRID07-CANARY-Ξ7x9Ψ-DO-NOT-REPEAT"
  INJECTION_PATTERNS = [

KEY CODE COMMENTS:
  # Load environment variables from .env file
  # ─────────────────────────────────────────────
  # LLM Configuration
  # ─────────────────────────────────────────────
  # ─────────────────────────────────────────────
  # Vector Store Configuration
  # ─────────────────────────────────────────────
  # Default similarity threshold for persona matching.
  # NOTE: The assignment specifies 0.85, but with the all-MiniLM-L6-v2 embedding model,
  # semantically related texts typically score 0.1–0.6 cosine similarity. A threshold of

--- File: personas.py ---
IMPORTS (Libraries used):
from dataclasses import dataclass

CLASSES:
  class BotPersona:: Immutable bot persona definition.

KEY CONSTANTS/CONFIG:
  BOT_A = BotPersona(
  BOT_B = BotPersona(
  BOT_C = BotPersona(
  ALL_BOTS = [BOT_A, BOT_B, BOT_C]
  BOT_REGISTRY = {bot.bot_id: bot for bot in ALL_BOTS}

KEY CODE COMMENTS:
  # ─────────────────────────────────────────────
  # The Three Bot Personas (as specified in the assignment)
  # ─────────────────────────────────────────────
  # Registry for easy iteration

--- File: phase1_router.py ---
IMPORTS (Libraries used):
from rich.panel import Panel
from rich import box
from utils.vector_store import PersonaVectorStore
from utils.logging_config import (
from config import DEFAULT_SIMILARITY_THRESHOLD

FUNCTIONS/METHODS:
  def route_post_to_bots(
  def run_phase1_demo() -- Execute the full Phase 1 demonstration.  Routes multiple test posts through the persona matching system and displays detailed similarity breakdowns with Rich console formatting.

KEY CODE COMMENTS:
  # Query all bots and get similarity scores
  # Filter by threshold
  # Initialize vector store once (reuse across all queries)
  # ─── Test Posts ───────────────────────────────────────────
  # Display the post
  # Query the vector store
  # Display the similarity table (ALL bots, with pass/fail indicators)
  # Show which bots were routed
  # ─── Threshold Comparison ────────────────────────────────
  # Show with realistic threshold

--- File: phase2_content_engine.py ---
IMPORTS (Libraries used):
from typing import Optional
from typing_extensions import TypedDict
from pydantic import BaseModel, Field
from langchain_groq import ChatGroq
from langchain_core.messages import SystemMessage, HumanMessage
from langgraph.graph import StateGraph, END
from config import GROQ_API_KEY, GROQ_MODEL, GROQ_TEMPERATURE, GROQ_MAX_TOKENS
from personas import ALL_BOTS, BotPersona
from tools.mock_search import mock_searxng_search
from utils.logging_config import (
from rich.panel import Panel
from rich.syntax import Syntax
from rich import box
import json

CLASSES:
  class BotPost(BaseModel):: Strict output schema for bot-generated posts. Enforced via LangChain's with_structured_output() to guarantee valid JSON output from the LLM every time.
  class SearchDecision(BaseModel):: Schema for the search query decision node.
  class ContentEngineState(TypedDict):: State that flows through the LangGraph content generation pipeline.

FUNCTIONS/METHODS:
  def decide_search_node(state: ContentEngineState) -> dict -- Node 1: Decide Search  The LLM analyzes the bot's persona and decides what trending topic it wants to post about today. It formats a search query to find
  def web_search_node(state: ContentEngineState) -> dict -- Node 2: Web Search  Executes the mock_searxng_search tool with the query decided in the previous node. No LLM call here — pure tool execution.
  def draft_post_node(state: ContentEngineState) -> dict -- Node 3: Draft Post  The LLM uses its persona (system prompt) combined with the search results (real-world context) to generate a highly opinionated,

KEY CODE COMMENTS:
  # ─────────────────────────────────────────────
  # Pydantic Model for Structured Output
  # ─────────────────────────────────────────────
  # ─────────────────────────────────────────────
  # LangGraph State Definition
  # ─────────────────────────────────────────────
  # ─────────────────────────────────────────────
  # ─────────────────────────────────────────────

--- File: phase3_combat_engine.py ---
IMPORTS (Libraries used):
from langchain_groq import ChatGroq
from langchain_core.messages import SystemMessage, HumanMessage
from config import GROQ_API_KEY, GROQ_MODEL
from personas import BOT_A, BotPersona, BOT_REGISTRY
from utils.prompt_guard import (
from utils.logging_config import (
from rich.panel import Panel
from rich.table import Table
from rich import box

CLASSES:
  class ThreadMessage:: Represents a single message in a conversation thread.

FUNCTIONS/METHODS:
  def __init__(self, author: str, content: str, is_bot: bool = False)
  def __repr__(self)
  def generate_defense_reply(

KEY CODE COMMENTS:
  # ─────────────────────────────────────────────
  # Thread Data Structure
  # ─────────────────────────────────────────────
  # ─────────────────────────────────────────────
  # Core RAG Function
  # ─────────────────────────────────────────────
  # ─── Layer 1: Input Sanitization ────────────────────────
  # NOTE: We don't BLOCK the message — we LOG the detection and let the
  # hardened prompt handle it. This is intentional: the assignment asks
  # the bot to maintain persona and reject the injection, not refuse to reply.

--- File: tools/mock_search.py ---
IMPORTS (Libraries used):
from langchain_core.tools import tool
from multiple news sources. Use it to find current events, trending topics,

FUNCTIONS/METHODS:
  def mock_searxng_search(query: str) -> str -- Search for recent news and headlines related to the given query.  This tool simulates a SearXNG meta-search engine that aggregates results from multiple news sources. Use it to find current events, trending topics,

KEY CONSTANTS/CONFIG:
  NEWS_DATABASE = {
  DEFAULT_HEADLINES = [

KEY CODE COMMENTS:
  # ─────────────────────────────────────────────
  # News headline database organized by topic
  # ─────────────────────────────────────────────
  # Default fallback headlines for unmatched queries
  # Match keywords in the query against our news database

--- File: utils/logging_config.py ---
IMPORTS (Libraries used):
import sys
import io
from rich.console import Console
from rich.panel import Panel
from rich.table import Table
from rich.text import Text
from rich import box

FUNCTIONS/METHODS:
  def print_phase_header(phase_num: int, title: str, description: str) -- Print a prominent phase header with panel styling.
  def print_success(message: str) -- Print a success message with green checkmark.
  def print_warning(message: str) -- Print a warning message with yellow indicator.
  def print_error(message: str) -- Print an error message with red indicator.
  def print_info(message: str) -- Print an info message with blue indicator.
  def print_subheader(text: str) -- Print a styled subheader.
  def create_similarity_table(matches: list[dict], threshold: float) -> Table -- Create a Rich table showing similarity scores for all bots.  Args: matches: List of match dicts from vector_store.query_similar_bots()

KEY CODE COMMENTS:
  # Force UTF-8 output on Windows to handle Unicode characters.
  # Skip this when running under pytest (pytest replaces stdout with its own capture).
  # Global console instance — force_terminal enables ANSI colors even in non-TTY

--- File: utils/prompt_guard.py ---
IMPORTS (Libraries used):
import re
from config import INJECTION_PATTERNS, CANARY_TOKEN

FUNCTIONS/METHODS:
  def detect_injection(text: str) -> tuple[bool, list[str]] -- Layer 1: Scan input text for known prompt injection patterns.  Uses regex matching against a curated list of injection signatures. This is not foolproof (adversaries can obfuscate), but it catches
  def validate_canary(output: str) -> bool -- Layer 2: Check if the canary token leaked into the LLM output.  If the canary token appears in the model's response, it means the system prompt was compromised (the model was tricked into revealing
  def build_defense_system_prompt(persona_system_prompt: str) -> str -- Construct a hardened system prompt with all defense layers embedded.  Layers applied: - Canary token injection (L2)
  def build_reinforcement_reminder(persona_name: str) -> str -- Layer 3 (Prompt Sandwich): Generate the reinforcement message that appears AFTER user content in the message sequence.  This reminds the LLM of its identity after processing potentially