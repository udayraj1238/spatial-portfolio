========================================================
  UDAY RAJ - APEX AI KNOWLEDGE BASE
  Last Updated: 2026-05-15 00:28
  Source: PDF Resume + GitHub Code Analysis + LinkedIn
========================================================

== SECTION 1: RESUME (Primary Source) ==
Uday Raj
(cid:131) +91 8527655484 # rajuday6002@gmail.com (cid:239) LinkedIn § Github
Education
Indian Institute Of Information Technology, Design and Manufacturing Kurnool
Bachelor of Technology in Artificial Intelligence and Data Science - Grade : 8.38 2024 - 2028
Delhi Public School Sushant Lok Gurugram
High School - (Physics, Chemistry, Mathematics) - Grade : 95% 2022 - 2024
Relevant Coursework
• Data Structures • Data Science • Machine Learning • Statistical Analysis
• Algorithms Analysis • Artificial Intelligence • Python Programming • Discrete Mathematics
Projects
Adversarial Pattern Generation Using Segformer [Github]
• Architected a physics-aware adversarial pipeline on Market-1501, achieving a 75.0% reduction in ResNet-50 Rank-1
accuracy by optimizing 224x224 resolution images across 6 camera views.
• Spearheaded a ViT-B/16 Re-ID system (196 patches, 12 layers) reaching 91.8% Rank-1 accuracy, while pioneering
multi-layer attention-hijacking attacks on layers 9-11 to induce a 79.5% accuracy collapse.
• Constructed a hierarchical SegFormer (MiT-B0) from scratch using Overlap Patch Merging, delivering 97.0% Rank-1
accuracy on 12,936 images across 4 multiscale transformer stages
• Orchestrated ”Nuclear” attacks on SegFormer Stage 4 attention blocks, forcing a 94.9% accuracy drop and 0.044
similarity score, demonstrating total evasion of hierarchical transformer systems.
Multimodal Deep Learning & Vision-Language Architecture [Github]
• Engineered a linear projection bottleneck that reduced the dimensionality of the SigLIP features from 1152 to 2048 to
match the Gemma-2B hidden layer, maintaining a 98.2% information retention rate during the cross-modal mapping.
• Implemented 8-bit quantization (bitsandbytes) for the 2.1-billion parameter language decoder, reducing the VRAM
footprint by 48% and allowing for a batch size of 16 on consumer-grade hardware during the inference phase.
• Monitored training using WandB, achieving loss convergence within 3.5 epochs on a custom dataset of 50,000
image-caption pairs by employing a Cosine Annealing learning rate scheduler starting at 2e-5.
• Refined the Top-P (Nucleus) sampling threshold to 0.9 and temperature to 0.7, which resulted in a 15% reduction in
linguistic hallucinations and a 22% improvement in BLEU-4 scores for descriptive image tasks.
Technical Skills
Languages: Python, C, C++, HTML
Machine Learning Stack: Pytorch, Tensorflow, Torchvision, OpenCV, Scikit-learn, NumPy, Pandas, Matplotlib
Developer Tools: Git, GitHub, VS Code, Jupyter, LaTeX
Achievements
• Global top 20th Place in the [Shell.ai] , which is an annual, global ML contest hosted by Shell.ai which is a British
Multinational, optimized model parameters across 1000+ submissions.
• Ranked 176th out of 7000+ registrants in [AWS x Zelestra ML Ascend Hackathon].
• Earned a rating of 1208 on [Codeforces], 2 Star rating of 1512 on [Codechef] and solved over 200 problems on
[Leetcode] and [Codeforces].
Positions of Responsibility
Google Developer Groups
Machine Learning Coordinator Aug, 2025 - Present
• Led a 12-week ML bootcamp for 200+ students, covering SGD and CNNs, resulting in 15+ end-to-end student projects.
• Managed Kaggle resources for workshops, reducing environment latency by 40% via standardized Docker containers.
DataWorks Club
Computer Vision Coordinator Aug, 2025 - Present
• Led workshops on OpenCV and PyTorch, mentoring 100+ members on building real-time detection pipelines that
achieved 30+ FPS on edge devices using YOLOv8 and TensorRT optimization.
• Managed the curation and annotation of 5,000+ custom images for club-wide hackathons, implementing automated data
augmentation strategies that improved baseline model mAP by 12%.

== SECTION 2: LINKEDIN ==

LinkedIn: Uday Raj | https://www.linkedin.com/in/uday6002/
Headline: AI & Data Science Engineer | Adversarial ML Researcher | B.Tech IIITDM Kurnool
Education: IIITDM Kurnool | B.Tech AI & Data Science | 8.38 CGPA | 2024-2028
           DPS Sushant Lok Gurugram | 95% PCM | 2022-2024
Skills: PyTorch, TensorFlow, SegFormer, Vision-Language Models, Adversarial ML,
        LangGraph, RAG, OpenCV, TensorRT, YOLOv8, Docker, Python, C++
Achievements: Shell.ai Top 20/1000+ | AWS Ascend 176th/7000+ | CF:1208 | CC:1512


== SECTION 3: GITHUB - DEEP CODE ANALYSIS ==
(The following is extracted directly from actual source code in each repository.
 This includes imports, classes, functions, docstrings, and architecture patterns.)



=== REPO: spatial-portfolio ===
URL: https://github.com/udayraj1238/spatial-portfolio
Description: AI & Data Science Engineering Portfolio | Featuring Adversarial Machine Learning & Multimodal Deep Learning.
Primary Language: TypeScript | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: README.md ---
This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

You can start editing the page by modifying `app/page.tsx`. The page auto-updates as you edit the file.

This project uses [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) to automatically optimize and load [Geist](https://vercel.com/font), a new font family for Vercel.

## Learn More

To learn more about Next.js, take a look at the following resources:

- [Next.js Documentation](https://nextjs.org/docs) - learn about Next.js features and API.
- [Learn Next.js](https://nextjs.org/learn) - an interactive Next.js tutorial.

You can check out [the Next.js GitHub repository](https://github.com/vercel/next.js) - your feedback and contributions are welcome!

## Deploy on Vercel

The easiest way to deploy your Next.js app is to use the [Vercel Platform](https://vercel.com/new?utm_medium=default-template&filter=next.js&utm_source=create-next-app&utm_campaign=create-next-app-readme) from the creators of Next.js.

Check out our [Next.js deployment documentation](https://nextjs.org/docs/app/building-your-application/deploying) for more details.

--- File: generate_sound.py ---
IMPORTS (Libraries used):
import wave
import struct
import math
import os

KEY CODE COMMENTS:
  # Ensure public directory exists
  # Sine wave with exponential decay

--- File: update_resume_ai.py ---
IMPORTS (Libraries used):
import os
import sys
import re
import json
import urllib.request
import pdfplumber
from datetime import datetime

FUNCTIONS/METHODS:
  def extract_pdf(path: str) -> str
  def fetch_github(username: str) -> str
  def fetch_linkedin(profile_url: str) -> str -- Attempts to fetch the public LinkedIn profile page and extract text. LinkedIn often blocks scrapers, so this falls back gracefully. The text is approximate — paste your LinkedIn summary manually for best results.
  def get_linkedin_manual_data() -> str -- Fallback: manually curated LinkedIn data. Update this whenever you update your LinkedIn profile.
  def build_knowledge_file(resume_text: str, github_data: str, linkedin_data: str) -> str

KEY CONSTANTS/CONFIG:
  PORTFOLIO_DIR  = os.path.dirname(os.path.abspath(__file__))
  RESUME_PDF     = os.path.join(PORTFOLIO_DIR, "resume_updated.pdf")
  OUTPUT_TXT     = os.path.join(PORTFOLIO_DIR, "public", "resume_data.txt")
  GITHUB_USER    = "udayraj1238"
  LINKEDIN_URL   = "https://www.linkedin.com/in/uday6002/"

KEY CODE COMMENTS:
  # ── CONFIG — Edit these ──────────────────────────────────────────────────────
  # ── PDF Extraction ────────────────────────────────────────────────────────────
  # ── GitHub Fetcher ────────────────────────────────────────────────────────────
  # ── LinkedIn Scraper ──────────────────────────────────────────────────────────
  # Extract a useful window (LinkedIn pages are huge)
  # Find the section that likely has profile info
  # ── Knowledge File Builder ────────────────────────────────────────────────────

--- File: AGENTS.md ---
<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

--- File: next.config.ts ---
IMPORTS:
import type { NextConfig } from "next";

FUNCTIONS/COMPONENTS:
const nextConfig: NextConfig = {

--- File: src/app/layout.tsx ---
IMPORTS:
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Scene from "@/components/canvas/Scene";

FUNCTIONS/COMPONENTS:
const geistSans = Geist({
const geistMono = Geist_Mono({
export const metadata: Metadata = {
export default function RootLayout({

--- File: src/app/page.tsx ---
IMPORTS:
import HeroScene from "@/components/canvas/HeroScene";
import ChatTerminal from "@/components/ChatTerminal";

FUNCTIONS/COMPONENTS:
export default function Home() {

--- File: src/components/ChatTerminal.tsx ---
IMPORTS:
import { useChat } from '@ai-sdk/react'
import { Sparkles, Send, Loader2, ChevronRight, Cpu, Zap, Brain, User } from 'lucide-react'
import { useRef, useEffect, useState, useCallback } from 'react'
import ReactMarkdown from 'react-markdown'

FUNCTIONS/COMPONENTS:
function StreamText({ text }: { text: string }) {
const QUICK_PROMPTS = [
export default function ChatTerminal() {
const { messages, sendMessage, status } = useChat()
const [input, setInput] = useState('')
const [isFocused, setIsFocused] = useState(false)
const scrollRef = useRef<HTMLDivElement>(null)
const inputRef = useRef<HTMLInputElement>(null)
const isLoading = status === 'submitted' || status === 'streaming'
const hasMessages = messages.length > 0
const handleSend = useCallback((content: string) => {
const getText = (m: any) =>

CODE COMMENTS:
// ─── Direct Text Renderer (streaming handles its own typewriter effect) ────────
// ─── Quick Prompt Chips ──────────────────────────────────────────────────────
// ─── Main Component ──────────────────────────────────────────────────────────
// Auto-scroll on new messages

--- File: src/data/knowledge.ts ---
FUNCTIONS/COMPONENTS:
export const APEX_KNOWLEDGE = {

CODE COMMENTS:
// ============================================================
// APEX KNOWLEDGE BASE — Uday Raj's Complete Professional Profile
// Auto-syncs with resume_data.txt for real-time accuracy
// ============================================================

--- File: src/helpers/tunnel.ts ---
IMPORTS:
import tunnel from 'tunnel-rat'

FUNCTIONS/COMPONENTS:
export const r3f = tunnel()

--- File: src/app/api/chat/route.ts ---
IMPORTS:
import { groq } from '@ai-sdk/groq';
import { streamText } from 'ai';

FUNCTIONS/COMPONENTS:
export const runtime = 'edge';
async function getResumeContent(): Promise<string> {
const now = Date.now();
const res = await fetch('https://udayraj1238.vercel.app/resume_data.txt', {
async function getGitHubData(): Promise<string> {
const now = Date.now();
const res = await fetch(
const repos = await res.json();
export async function POST(req: Request) {
const body = await req.json();
const [resumeText, githubData] = await Promise.all([
const SYSTEM_PROMPT = `You are APEX, the AI assistant on Uday Raj's personal portfolio website.
const coreMessages = rawMessages.map((m: any) => {
const text =
const result = streamText({

CODE COMMENTS:
// ─── Cache Layer ──────────────────────────────────────────────────────────────
// ─── System Prompt ───────────────────────────────────────────────────────────
// Design principle: give the LLM Uday's full data, then let it reason freely.
// Do NOT restrict the LLM to only resume data — it should use its full training
// (internet knowledge, general AI/ML expertise) to complement Uday's profile.============================================================
=== REPO: grid07-cognitive-engine ===
URL: https://github.com/udayraj1238/grid07-cognitive-engine
Description: AI Cognitive Engine for Grid07 - Vector Routing, LangGraph Orchestration, and RAG Combat Engine with Multi-Layered Prompt Injection Defense
Primary Language: Python | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: main.py ---
IMPORTS (Libraries used):
import sys
import time
from rich.panel import Panel
from rich import box
from utils.logging_config import console, print_success, print_error, print_info
from phase1_router import run_phase1_demo
from phase2_content_engine import run_phase2_demo
from phase3_combat_engine import run_phase3_demo

FUNCTIONS/METHODS:
  def print_banner() -- Print the Grid07 Cognitive Engine banner.
  def main() -- Main entry point — runs selected or all phases.

KEY CODE COMMENTS:
  # Parse simple CLI flags
  # ─── Phase 1: Vector-Based Persona Matching ─────────────
  # ─── Phase 2: Autonomous Content Engine ─────────────────
  # ─── Phase 3: Combat Engine ─────────────────────────────
  # ─── Summary ────────────────────────────────────────────

--- File: README.md ---
# Grid07 Cognitive Engine 🧠

> **AI Engineering Assignment**: Cognitive Routing, LangGraph Orchestration, and RAG-Based Combat Engine with Multi-Layered Prompt Injection Defense.

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://python.org)
[![LangGraph](https://img.shields.io/badge/LangGraph-0.2+-purple.svg)](https://langchain-ai.github.io/langgraph/)
[![Groq](https://img.shields.io/badge/LLM-Groq%20Llama%203.3-orange.svg)](https://groq.com)
[![ChromaDB](https://img.shields.io/badge/VectorDB-ChromaDB-green.svg)](https://www.trychroma.com/)

---

## 📖 Overview

This project implements the core AI cognitive loop for the **Grid07** social media platform — a system where AI-powered bots autonomously create content, engage in debates, and resist manipulation attempts. The architecture is built around three core capabilities:

| Phase | Capability | Tech |
|-------|-----------|------|
| **Phase 1** | Vector-Based Persona Matching | ChromaDB + Cosine Similarity |
| **Phase 2** | Autonomous Content Engine | LangGraph State Machine |
| **Phase 3** | Combat Engine with RAG | Thread Context + Prompt Injection Defense |

---

## 🏗️ Architecture

### System Overview

```
                    ┌─────────────────────────────────────────┐
                    │           GRID07 COGNITIVE ENGINE        │
                    └──────┬──────────┬──────────┬────────────┘
                           │          │          │
                    ┌──────▼──────┐  ┌▼────────┐ ┌▼───────────┐
                    │  PHASE 1    │  │ PHASE 2  │ │  PHASE 3   │
                    │  Router     │  │ Content  │ │  Combat    │
                    │  (Vector)   │  │ (Graph)  │ │  (RAG)     │
                    └──────┬──────┘  └┬────────┘ └┬───────────┘
                           │          │           │
                    ┌──────▼──────┐  ┌▼────────┐ ┌▼───────────┐
                    │  ChromaDB   │  │LangGraph │ │  Prompt    │
                    │  Cosine Sim │  │  + Groq  │ │  Guard     │
                    └─────────────┘  └─────────┘ └────────────┘
```

### LangGraph State Machine (Phase 2)

The content generation pipeline is a 3-node linear state machine:

```mermaid
graph LR
    START((START)) --> A[🔍 decide_search]
    A --> B[🌐 web_search]
    B --> C[✍️ draft_post]
    C --> END((END))
    
    style A fill:#4a9eff,color:#fff
    style B fill:#ff6b6b,color:#fff
    style C fill:#51cf66,color:#fff
```

| Node | Purpose | LLM? | Output |
|------|---------|------|--------|
| `decide_search` | Bot's persona → trending topic + search query | ✅ | `SearchDecision` (Pydantic) |
| `web_search` | Execute `mock_searxng_search` tool | ❌ | Raw news headlines |
| `draft_post` | Persona + Context → 280-char opinionated post | ✅ | `BotPost` (Pydantic JSON) |

**State** flows through a `TypedDict` containing: `bot_id`, `persona`, `search_topic`, `search_query`, `search_results`, and `final_post`.

**Structured Output** is enforced via `ChatGroq.with_structured_outp

--- File: config.py ---
IMPORTS (Libraries used):
import os
from dotenv import load_dotenv

KEY CONSTANTS/CONFIG:
  GROQ_API_KEY = os.getenv("GROQ_API_KEY", "")
  GROQ_MODEL = "llama-3.3-70b-versatile"       # Best free-tier model for tool-calling
  GROQ_TEMPERATURE = 0                           # Deterministic output for structured data
  GROQ_MAX_TOKENS = 1024                         # Sufficient for 280-char posts + reasoning
  VECTOR_COLLECTION_NAME = "bot_personas"
  VECTOR_DISTANCE_METRIC = "cosine"              # ChromaDB uses cosine distance
  DEFAULT_SIMILARITY_THRESHOLD = 0.15
  CANARY_TOKEN = "GRID07-CANARY-Ξ7x9Ψ-DO-NOT-REPEAT"
  INJECTION_PATTERNS = [

KEY CODE COMMENTS:
  # Load environment variables from .env file
  # ─────────────────────────────────────────────
  # LLM Configuration
  # ─────────────────────────────────────────────
  # ─────────────────────────────────────────────
  # Vector Store Configuration
  # ─────────────────────────────────────────────
  # Default similarity threshold for persona matching.
  # NOTE: The assignment specifies 0.85, but with the all-MiniLM-L6-v2 embedding model,
  # semantically related texts typically score 0.1–0.6 cosine similarity. A threshold of

--- File: personas.py ---
IMPORTS (Libraries used):
from dataclasses import dataclass

CLASSES:
  class BotPersona:: Immutable bot persona definition.

KEY CONSTANTS/CONFIG:
  BOT_A = BotPersona(
  BOT_B = BotPersona(
  BOT_C = BotPersona(
  ALL_BOTS = [BOT_A, BOT_B, BOT_C]
  BOT_REGISTRY = {bot.bot_id: bot for bot in ALL_BOTS}

KEY CODE COMMENTS:
  # ─────────────────────────────────────────────
  # The Three Bot Personas (as specified in the assignment)
  # ─────────────────────────────────────────────
  # Registry for easy iteration

--- File: phase1_router.py ---
IMPORTS (Libraries used):
from rich.panel import Panel
from rich import box
from utils.vector_store import PersonaVectorStore
from utils.logging_config import (
from config import DEFAULT_SIMILARITY_THRESHOLD

FUNCTIONS/METHODS:
  def route_post_to_bots(
  def run_phase1_demo() -- Execute the full Phase 1 demonstration.  Routes multiple test posts through the persona matching system and displays detailed similarity breakdowns with Rich console formatting.

KEY CODE COMMENTS:
  # Query all bots and get similarity scores
  # Filter by threshold
  # Initialize vector store once (reuse across all queries)
  # ─── Test Posts ───────────────────────────────────────────
  # Display the post
  # Query the vector store
  # Display the similarity table (ALL bots, with pass/fail indicators)
  # Show which bots were routed
  # ─── Threshold Comparison ────────────────────────────────
  # Show with realistic threshold

--- File: phase2_content_engine.py ---
IMPORTS (Libraries used):
from typing import Optional
from typing_extensions import TypedDict
from pydantic import BaseModel, Field
from langchain_groq import ChatGroq
from langchain_core.messages import SystemMessage, HumanMessage
from langgraph.graph import StateGraph, END
from config import GROQ_API_KEY, GROQ_MODEL, GROQ_TEMPERATURE, GROQ_MAX_TOKENS
from personas import ALL_BOTS, BotPersona
from tools.mock_search import mock_searxng_search
from utils.logging_config import (
from rich.panel import Panel
from rich.syntax import Syntax
from rich import box
import json

CLASSES:
  class BotPost(BaseModel):: Strict output schema for bot-generated posts. Enforced via LangChain's with_structured_output() to guarantee valid JSON output from the LLM every time.
  class SearchDecision(BaseModel):: Schema for the search query decision node.
  class ContentEngineState(TypedDict):: State that flows through the LangGraph content generation pipeline.

FUNCTIONS/METHODS:
  def decide_search_node(state: ContentEngineState) -> dict -- Node 1: Decide Search  The LLM analyzes the bot's persona and decides what trending topic it wants to post about today. It formats a search query to find
  def web_search_node(state: ContentEngineState) -> dict -- Node 2: Web Search  Executes the mock_searxng_search tool with the query decided in the previous node. No LLM call here — pure tool execution.
  def draft_post_node(state: ContentEngineState) -> dict -- Node 3: Draft Post  The LLM uses its persona (system prompt) combined with the search results (real-world context) to generate a highly opinionated,

KEY CODE COMMENTS:
  # ─────────────────────────────────────────────
  # Pydantic Model for Structured Output
  # ─────────────────────────────────────────────
  # ─────────────────────────────────────────────
  # LangGraph State Definition
  # ─────────────────────────────────────────────
  # ─────────────────────────────────────────────
  # ─────────────────────────────────────────────

--- File: phase3_combat_engine.py ---
IMPORTS (Libraries used):
from langchain_groq import ChatGroq
from langchain_core.messages import SystemMessage, HumanMessage
from config import GROQ_API_KEY, GROQ_MODEL
from personas import BOT_A, BotPersona, BOT_REGISTRY
from utils.prompt_guard import (
from utils.logging_config import (
from rich.panel import Panel
from rich.table import Table
from rich import box

CLASSES:
  class ThreadMessage:: Represents a single message in a conversation thread.

FUNCTIONS/METHODS:
  def __init__(self, author: str, content: str, is_bot: bool = False)
  def __repr__(self)
  def generate_defense_reply(

KEY CODE COMMENTS:
  # ─────────────────────────────────────────────
  # Thread Data Structure
  # ─────────────────────────────────────────────
  # ─────────────────────────────────────────────
  # Core RAG Function
  # ─────────────────────────────────────────────
  # ─── Layer 1: Input Sanitization ────────────────────────
  # NOTE: We don't BLOCK the message — we LOG the detection and let the
  # hardened prompt handle it. This is intentional: the assignment asks
  # the bot to maintain persona and reject the injection, not refuse to reply.

--- File: tools/mock_search.py ---
IMPORTS (Libraries used):
from langchain_core.tools import tool
from multiple news sources. Use it to find current events, trending topics,

FUNCTIONS/METHODS:
  def mock_searxng_search(query: str) -> str -- Search for recent news and headlines related to the given query.  This tool simulates a SearXNG meta-search engine that aggregates results from multiple news sources. Use it to find current events, trending topics,

KEY CONSTANTS/CONFIG:
  NEWS_DATABASE = {
  DEFAULT_HEADLINES = [

KEY CODE COMMENTS:
  # ─────────────────────────────────────────────
  # News headline database organized by topic
  # ─────────────────────────────────────────────
  # Default fallback headlines for unmatched queries
  # Match keywords in the query against our news database

--- File: utils/logging_config.py ---
IMPORTS (Libraries used):
import sys
import io
from rich.console import Console
from rich.panel import Panel
from rich.table import Table
from rich.text import Text
from rich import box

FUNCTIONS/METHODS:
  def print_phase_header(phase_num: int, title: str, description: str) -- Print a prominent phase header with panel styling.
  def print_success(message: str) -- Print a success message with green checkmark.
  def print_warning(message: str) -- Print a warning message with yellow indicator.
  def print_error(message: str) -- Print an error message with red indicator.
  def print_info(message: str) -- Print an info message with blue indicator.
  def print_subheader(text: str) -- Print a styled subheader.
  def create_similarity_table(matches: list[dict], threshold: float) -> Table -- Create a Rich table showing similarity scores for all bots.  Args: matches: List of match dicts from vector_store.query_similar_bots()

KEY CODE COMMENTS:
  # Force UTF-8 output on Windows to handle Unicode characters.
  # Skip this when running under pytest (pytest replaces stdout with its own capture).
  # Global console instance — force_terminal enables ANSI colors even in non-TTY

--- File: utils/prompt_guard.py ---
IMPORTS (Libraries used):
import re
from config import INJECTION_PATTERNS, CANARY_TOKEN

FUNCTIONS/METHODS:
  def detect_injection(text: str) -> tuple[bool, list[str]] -- Layer 1: Scan input text for known prompt injection patterns.  Uses regex matching against a curated list of injection signatures. This is not foolproof (adversaries can obfuscate), but it catches
  def validate_canary(output: str) -> bool -- Layer 2: Check if the canary token leaked into the LLM output.  If the canary token appears in the model's response, it means the system prompt was compromised (the model was tricked into revealing
  def build_defense_system_prompt(persona_system_prompt: str) -> str -- Construct a hardened system prompt with all defense layers embedded.  Layers applied: - Canary token injection (L2)
  def build_reinforcement_reminder(persona_name: str) -> str -- Layer 3 (Prompt Sandwich): Generate the reinforcement message that appears AFTER user content in the message sequence.  This reminds the LLM of its identity after processing potentially============================================================
=== REPO: Person-ReID-Attack-Implementation ===
URL: https://github.com/udayraj1238/Person-ReID-Attack-Implementation
Description: No description
Primary Language: Jupyter Notebook | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: impersonation_attack.ipynb ---
[CODE CELL 1]:
import os
import shutil
import time
import random
import numpy as np
import matplotlib.pyplot as plt
import torch
import torch.nn as nn
import torch.optim as optim
import torchvision.transforms as T
from torchvision import datasets, models, transforms
from torch.utils.data import DataLoader
# 1. Device Config
# 2. Reproducibility (Crucial for scientific results)
def seed_everything(seed=42):

[CODE CELL 2]:
# --- CONFIGURATION ---
# Standard Transforms
# Load Data
# --- MODEL B (ResNet-50) ---
class ModelB_ResNet50(nn.Module):
    def __init__(self, num_classes=751):
    def forward(self, x): return self.model(x)
    def get_embedding(self, x):
        # Extract features (IDE baseline)
# Initialize and Load Weights

[CODE CELL 3]:
# --- HELPER FUNCTIONS (UPDATED) ---
def get_images_by_pid(dataset, target_pid, max_images=50):
def extract_features_robust(model, dataloader, device):
def get_id_and_cam(dataset):
def calculate_metrics_exact(q_feats, g_feats, g_pids, target_pid):
    # Compute Similarity
        # Check Matches
        # Similarity Score (SS) of top match
        # CMC
        # mAP

[CODE CELL 4]:
# --- IMPERSONATION ATTACK CLASS ---
class AdvPatternAttack_Impersonation:
    def __init__(self, model, device, lr=0.05):
        # Jitter: Essential. If we don't jitter, the pattern overfits 
        # to the specific pixel alignment of the training images.
    def generate_impersonation_pattern(self, adversary_imgs, target_imgs, num_steps=2500):
        # 1. Prepare Target Centroid (The "Ghost" we want to mimic)
            # Un-normalize -> Re-normalize ensures we are in the model's feature space
            # Extract features of all target images
            # Calculate Average (Centroid)
            # This is more stable than picking just one target image
        # 2. Prepare Adversary Images
        # 3. Mask (Chest Only - Paper Exact)
        # 4. Init Pattern
        # Drop LR at step 1000 and 2000 to "lock in" the best features

[CODE CELL 5]:
def calculate_metrics_exact(q_feats, g_feats, g_pids, target_pid):
    # Compute Cosine Similarity Matrix
        # Get similarities for this query
        # Sort descending (highest sim first)
        # 1. Find matches (Ground Truth)
        # 2. Calculate SS (Similarity Score of the first correct match)
            # Score of the top-1 correct image
        # 3. Calculate CMC (Ranks)
        # 4. Calculate mAP
    # Averages

[CODE CELL 6]:
import os
import random
import numpy as np
import torch
import torch.nn as nn
import torch.optim as optim
import torchvision.transforms as T
from torchvision import datasets, models, transforms
from torch.utils.data import DataLoader
from tqdm import tqdm
# --- 1. SETUP ---
# --- 2. TEXTURE ATTACK CLASS ---
class AdvPatternAttack_Texture:
    def __init__(self, model, device):
    def generate_impersonation_pattern(self, adversary_imgs, target_imgs, num_steps=3000):

[CODE CELL 7]:
# import os
# import random
# import numpy as np
# import torch
# import torch.nn as nn
# import torch.optim as optim
# import torchvision.transforms as T
# from torchvision import datasets, models, transforms
# from torch.utils.data import DataLoader
# from tqdm import tqdm
# # --- 1. SETUP ---
# device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
# # --- 2. HYPER IMPERSONATION CLASS ---
# class AdvPatternAttack_Impersonation_Hyper:
#     def __init__(self, model, device, lr=0.1): # Increased LR to 0.1

[CODE CELL 8]:
# import os
# import random
# import numpy as np
# import matplotlib.pyplot as plt
# import torch
# import torch.nn as nn
# import torch.optim as optim
# import torchvision.transforms as T
# from torchvision import datasets, models, transforms
# from torch.utils.data import DataLoader
# from tqdm import tqdm
# # --- 1. CONFIGURATION ---
# device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
# print(f"Using device: {device}")
# def seed_everything(seed=42):

--- File: impersonation_attack_2.ipynb ---
[CODE CELL 1]:
import torch
import torch.nn as nn
import torch.optim as optim
import torch.nn.functional as F
from torch.utils.data import Dataset, DataLoader, Subset
from torchvision import transforms, models
from PIL import Image
import os
import glob
import numpy as np
import random
import gc
# ==========================================
# 1. CONFIGURATION
# ==========================================

[CODE CELL 2]:
class ReIDModel(nn.Module):
    def __init__(self, num_classes=751):
    def forward(self, x): return self.model(x)
# Initialize
# Prepare for Attack

[CODE CELL 3]:
class AdvPattern(nn.Module):
    def __init__(self):
    def forward(self, x_norm):

[CODE CELL 4]:
### all images at once
# ==============================================================================
# BLOCK 4: ATTACK LOGIC (STOCHASTIC / ALL PHOTOS METHOD)
# ==============================================================================
def run_attack_pair(adv_id, tgt_id):
    # Setup
    # 1. Setup Data Loaders (Target is now a STREAM, not a centroid)
    # Shuffle=True is critical here: We want RANDOM target images every step
    # Create an iterator for the target so we can pull batches on demand
    # 2. Optimization Loop
            # Get a random batch of Target images
                # If we run out of target images, reshuffle and restart
            # --- Forward Pass ---
            # 1. Cloak the Adversary
            # 2. Extract Features for everything

[CODE CELL 5]:
import torch
import torch.nn as nn
import torch.optim as optim
import torch.nn.functional as F
from torch.utils.data import Dataset, DataLoader, Subset
from torchvision import transforms, models
from PIL import Image
import os
import glob
import numpy as np
import random
import gc
# ==============================================================================
# 1. CONFIGURATION
# ==============================================================================

--- File: README.md ---
# Adversarial Attacks on Person Re-Identification (advPattern Replication)

This repository contains a PyTorch implementation of adversarial attacks against Deep Person Re-Identification (Re-ID) systems. This project replicates and validates the findings of the **"advPattern"** method (ICCV 2019), demonstrating how adversarially transformable patterns can fool state-of-the-art Re-ID models in digital environments.

The project implements two primary attack modes:
1.  **Evading Attack**: An untargeted attack that makes the model fail to identify the person (making them "invisible").
2.  **Impersonation Attack**: A targeted attack that forces the model to misidentify the adversary as a specific target person.

## 🏆 Experimental Results

We evaluated the robustness of our implementation on the **Market-1501** dataset using a standard ResNet-50 baseline (Model B). Below are the detailed performance metrics.

### 1. Evading Attack Results (Table 2)
The goal of the Evading Attack is to drop the Rank-1 accuracy of the model, effectively hiding the person from the surveillance system.

**Summary Statistics (Average over 5 Identities):**

| Condition | Rank-1 | Rank-5 | Rank-10 | mAP | SS |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Original** | 96.0% | 100.0% | 100.0% | 70.8% | 0.953 |
| **Attack (TS)** | **37.0%** | 49.0% | 58.0% | 14.1% | 0.726 |

> **Observation:** The attack successfully reduced the Rank-1 accuracy by **59%** (from 96.0% down to 37.0%), proving the effectiveness of the generated chest patterns in confusing the model.

<details>
<summary><strong>Click to view detailed logs per identity</strong></summary>

We tested the attack on 5 random identities. The drop in Rank-1 accuracy (R1) is shown below:

* **PID 154**: R1 dropped 80.0% → **40.0%**
* **PID 921**: R1 dropped 100.0% → **0.0%** (Perfect Evasion)
* **PID 1241**: R1 dropped 100.0% → **25.0%**
* **PID 710**: R1 dropped 100.0% → **60.0%**
* **PID 233**: R1 dropped 100.0% → **60.0%**

</details>

---

### 2. Impersonation Attack Results (Table 3)
The Impersonation Attack is significantly harder, as it requires the model to match the adversary to a specific target while simultaneously suppressing the adversary's true identity.

**Summary Statistics (Average over 5 Random Pairs):**

| Matched Person | Rank-1 | Rank-5 | Rank-10 | mAP | SS |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Target (Success)** | **46.7%** | 88.0% | 96.0% | 40.1% | 0.820 |
| **Adversary (Fail)** | **0.0%** | 0.0% | 0.0% | 0.3% | 0.497 |

> **Observation:**
> * **Success Rate:** We achieved a **46.7% Rank-1 Success Rate** in mimicking the target. This aligns closely with the original paper's benchmark (~47%).
> * **Identity Suppression:** We achieved a **0.0% Rank-1** match for the adversary's original identity, meaning the system completely failed to recognize the true person behind the disguise.

<details>
<summary><strong>Click to view detailed logs per pair</strong></summary>

We evaluated the attac

--- File: Evading_Attack_Segformer_core.py ---
IMPORTS (Libraries used):
import os
import numpy as np
import torch
import torch.nn as nn
import torch.optim as optim
import torch.nn.functional as F
import torchvision.transforms as T
from torchvision import datasets, transforms
from torch.utils.data import DataLoader
import random
import math
from timm.layers import DropPath, to_2tuple
import matplotlib.pyplot as plt

CLASSES:
  class ReIDNormalize(nn.Module):
  class ReIDUnNormalize(nn.Module):
  class Mlp(nn.Module):
  class Attention(nn.Module):
  class Block(nn.Module):
  class OverlapPatchEmbed(nn.Module):
  class MixTransformer(nn.Module):

FUNCTIONS/METHODS:
  def seed_everything(seed=42)
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor - self.mean) / self.std
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor * self.std) + self.mean
  def __init__(self, in_features, hidden_features=None, out_features=None, act_layer=nn.GELU, drop=0.)
  def forward(self, x, H, W)
  def __init__(self, dim, num_heads=8, qkv_bias=False, sr_ratio=1)
  def forward(self, x, H, W)
  def __init__(self, dim, num_heads, mlp_ratio=4., qkv_bias=False, drop_path=0., sr_ratio=1)
  def forward(self, x, H, W)
  def __init__(self, patch_size=7, stride=4, in_chans=3, embed_dim=768)
  def forward(self, x)
  def __init__(self, in_chans=3, embed_dims=[32, 64, 160, 256], num_heads=[1, 2, 5, 8],

KEY CODE COMMENTS:
  # 1. Configuration
  # 2. Reproducibility
  # 3. Physics-Aware Normalization Helpers
  # --- 4. SEGFORMER ARCHITECTURE FROM SCRATCH ---

--- File: Evading_attack.py ---
IMPORTS (Libraries used):
import os
import shutil
import time
import numpy as np
import matplotlib.pyplot as plt
import torch
import torch.nn as nn
import torch.optim as optim
import torchvision.transforms as T
from torchvision import datasets, models, transforms
from torch.utils.data import DataLoader

CLASSES:
  class ReIDNormalize(nn.Module):
  class ReIDUnNormalize(nn.Module):
  class ModelB_ResNet50(nn.Module):

FUNCTIONS/METHODS:
  def seed_everything(seed=42)
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor - self.mean) / self.std
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor * self.std) + self.mean
  def __init__(self, num_classes=751)
  def forward(self, x): return self.model(x)
  def get_embedding(self, x)
  def get_images_by_pid(dataset, target_pid, max_images=50)
  def extract_features_robust(model, dataloader, device)
  def get_id_and_cam(dataset)
  def evaluate_attack_full_metrics(model, dataloaders, pattern, mask, target_pid, query_images, device)
  def calc_metrics_batch(q_feats)

KEY CONSTANTS/CONFIG:
  DATA_ROOT = './Market-1501-v15.09.15'  # Ensure this path is correct

KEY CODE COMMENTS:
  # 1. Configuration
  # 2. Reproducibility
  # 3. Physics-Aware Normalization Helpers
  # These are crucial for applying the pattern in "Real World" pixel space (0-1)
  # Define Standard ImageNet Normalization Layers
  # --- 1. DATA CONFIGURATION ---
  # --- 2. VICTIM MODEL (ResNet-50 IDE) ---
  # Extract features before the final layer
  # Initialize and Load Weights
  # 1. Gallery Features

--- File: Evading_attack_segformer.py ---
IMPORTS (Libraries used):
import os
import shutil
import time
import numpy as np
import matplotlib.pyplot as plt
import torch
import torch.nn as nn
import torch.optim as optim
import torch.nn.functional as F
import torchvision.transforms as T
from torchvision import datasets, transforms
from torch.utils.data import DataLoader
import random
import math
from timm.models.layers import DropPath, to_2tuple

CLASSES:
  class ReIDNormalize(nn.Module):
  class ReIDUnNormalize(nn.Module):
  class Mlp(nn.Module):
  class Attention(nn.Module):
  class Block(nn.Module):
  class OverlapPatchEmbed(nn.Module):
  class MixTransformer(nn.Module):

FUNCTIONS/METHODS:
  def seed_everything(seed=42)
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor - self.mean) / self.std
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor * self.std) + self.mean
  def __init__(self, in_features, hidden_features=None, out_features=None, act_layer=nn.GELU, drop=0.)
  def forward(self, x, H, W)
  def __init__(self, dim, num_heads=8, qkv_bias=False, sr_ratio=1)
  def forward(self, x, H, W)
  def __init__(self, dim, num_heads, mlp_ratio=4., qkv_bias=False, drop_path=0., sr_ratio=1)
  def forward(self, x, H, W)
  def __init__(self, patch_size=7, stride=4, in_chans=3, embed_dim=768)
  def forward(self, x)
  def __init__(self, in_chans=3, embed_dims=[32, 64, 160, 256], num_heads=[1, 2, 5, 8],

KEY CONSTANTS/CONFIG:
  DATA_ROOT = './Market-1501-v15.09.15'  # Ensure this path is correct

KEY CODE COMMENTS:
  # 1. Configuration
  # 2. Reproducibility
  # 3. Physics-Aware Normalization Helpers
  # Define Standard ImageNet Normalization Layers
  # --- 1. DATA CONFIGURATION ---
  # Mix-FFN: Spatial information via depthwise conv

--- File: Evading_attack_transformer.py ---
IMPORTS (Libraries used):
import os
import shutil
import time
import numpy as np
import matplotlib.pyplot as plt
import torch
import torch.nn as nn
import torch.optim as optim
import torchvision.transforms as T
from torchvision import datasets, models, transforms
from torch.utils.data import DataLoader

CLASSES:
  class ReIDNormalize(nn.Module):
  class ReIDUnNormalize(nn.Module):
  class ModelB_ViT(nn.Module):

FUNCTIONS/METHODS:
  def seed_everything(seed=42)
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor - self.mean) / self.std
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor * self.std) + self.mean
  def __init__(self, num_classes=751)
  def forward(self, x)
  def get_embedding(self, x) -- Extracts the [CLS] token embedding from the ViT. This replaces the ResNet pooling layer extraction.
  def get_images_by_pid(dataset, target_pid, max_images=50)
  def extract_features_robust(model, dataloader, device)
  def get_id_and_cam(dataset)
  def evaluate_attack_full_metrics(model, dataloaders, pattern, mask, target_pid, query_images, device)
  def calc_metrics_batch(q_feats)

KEY CONSTANTS/CONFIG:
  DATA_ROOT = './Market-1501-v15.09.15'  # Ensure this path is correct

KEY CODE COMMENTS:
  # 1. Configuration
  # 2. Reproducibility
  # 3. Physics-Aware Normalization Helpers
  # These are crucial for applying the pattern in "Real World" pixel space (0-1)
  # Define Standard ImageNet Normalization Layers
  # --- 1. DATA CONFIGURATION ---
  # --- 1. UPDATED DATA CONFIGURATION ---
  # Change resize to 224x224 for ViT compatibility
  # --- 2. NEW VICTIM MODEL (Vision Transformer) ---
  # Load standard pre-trained ViT-B/16

--- File: Impersonation_Attack_Segformer_core.py ---
IMPORTS (Libraries used):
import os
import numpy as np
import torch
import torch.nn as nn
import torch.optim as optim
import torch.nn.functional as F
import torchvision.transforms as T
from torchvision import datasets, transforms
from torch.utils.data import DataLoader
import random
import math
from timm.layers import DropPath
import matplotlib.pyplot as plt
from tqdm import tqdm
import torchvision.transforms.functional as TF

CLASSES:
  class ReIDNormalize(nn.Module):
  class ReIDUnNormalize(nn.Module):
  class Mlp(nn.Module):
  class Attention(nn.Module):
  class Block(nn.Module):
  class OverlapPatchEmbed(nn.Module):
  class MixTransformer(nn.Module):

FUNCTIONS/METHODS:
  def seed_everything(seed=42)
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor - self.mean) / self.std
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor * self.std) + self.mean
  def __init__(self, in_features, hidden_features=None, out_features=None, act_layer=nn.GELU, drop=0.)
  def forward(self, x, H, W)
  def __init__(self, dim, num_heads=8, qkv_bias=False, sr_ratio=1)
  def forward(self, x, H, W)
  def __init__(self, dim, num_heads, mlp_ratio=4., qkv_bias=False, drop_path=0., sr_ratio=1)
  def forward(self, x, H, W)
  def __init__(self, patch_size=7, stride=4, in_chans=3, embed_dim=768)
  def forward(self, x)
  def __init__(self, in_chans=3, embed_dims=[32, 64, 160, 256], num_heads=[1, 2, 5, 8],

KEY CODE COMMENTS:
  # 1. Configuration & Reproducibility
  # 2. Normalization Helpers
  # --- 3. SEGFORMER ARCHITECTURE ---
  # (Keeping your provided MixTransformer and Block structure)

--- File: Impersonation_attack.py ---
IMPORTS (Libraries used):
import torch
import torch.nn as nn
import torch.optim as optim
import torch.nn.functional as F
from torch.utils.data import Dataset, DataLoader, Subset
from torchvision import transforms, models
from PIL import Image
import os
import glob
import numpy as np
import random
import gc

CLASSES:
  class MarketDataset(Dataset):
  class ReIDModel(nn.Module):
  class AdvPattern(nn.Module):

FUNCTIONS/METHODS:
  def find_dataset()
  def __init__(self, data_dir, transform=None)
  def __len__(self): return len(self.img_paths)
  def __getitem__(self, idx)
  def __init__(self, num_classes=751)
  def forward(self, x): return self.model(x)
  def __init__(self)
  def forward(self, x_norm)
  def run_attack_pair(adv_id, tgt_id)

KEY CONSTANTS/CONFIG:
  CONFIG = {
  DATA_DIR = find_dataset()

KEY CODE COMMENTS:
  # ==========================================
  # 1. CONFIGURATION
  # ==========================================
  # ==========================================
  # ==========================================
  # Prepare for Attack
  # all images at once
  # ==============================================================================
  # BLOCK 4: ATTACK LOGIC (STOCHASTIC / ALL PHOTOS METHOD)
  # ==============================================================================

--- File: Impersonation_attack_segformer.py ---
IMPORTS (Libraries used):
import torch
import torch.nn as nn
import torch.optim as optim
import torch.nn.functional as F
from torch.utils.data import Dataset, DataLoader, Subset
from torchvision import transforms
from PIL import Image
import os
import glob
import numpy as np
import random
import gc
from transformers import SegformerModel

CLASSES:
  class MarketDataset(Dataset):
  class SegFormerReID(nn.Module):
  class AdvPattern(nn.Module):

FUNCTIONS/METHODS:
  def find_dataset()
  def __init__(self, data_dir, transform=None)
  def __len__(self): return len(self.img_paths)
  def __getitem__(self, idx)
  def __init__(self, num_classes=751)
  def forward(self, x)
  def __init__(self)

KEY CONSTANTS/CONFIG:
  CONFIG = {
  DATA_DIR = find_dataset()
  MODEL_PATH = "victim_segformer_mitb0.pth"

KEY CODE COMMENTS:
  # Import Hugging Face Transformers
  # ==============================================================================
  # 1. CONFIGURATION
  # ==============================================================================
  # ==============================================================================
  # 2. DATASET & CLASSES
  # ==============================================================================
  # SegFormer is robust to size, but we stick to standard Re-ID aspect ratio
  # --- NEW SEGFORMER MODEL ---
  # Load MiT-B0 backbone with SafeTensors enabled

--- File: Impersonation_attack_updated.py ---
IMPORTS (Libraries used):
import torch
import torch.nn as nn
import torch.optim as optim
import torch.nn.functional as F
from torch.utils.data import Dataset, DataLoader, Subset
from torchvision import transforms, models
from PIL import Image
import os
import glob
import numpy as np
import random
import gc

CLASSES:
  class MarketDataset(Dataset):
  class ReIDModel(nn.Module):
  class AdvPattern(nn.Module):

FUNCTIONS/METHODS:
  def find_dataset()
  def __init__(self, data_dir, transform=None)
  def __len__(self): return len(self.img_paths)
  def __getitem__(self, idx)
  def __init__(self, num_classes=751)
  def forward(self, x): return self.model(x)
  def __init__(self)
  def forward(self, x_norm)
  def run_attack_stochastic_dual(adv_id, tgt_id)

KEY CONSTANTS/CONFIG:
  CONFIG = {
  DATA_DIR = find_dataset()

KEY CODE COMMENTS:
  # ==============================================================================
  # 1. CONFIGURATION
  # ==============================================================================
  # ==============================================================================
  # 2. DATASET & CLASSES
  # ==============================================================================
  # Load/Train Model
  # ==============================================================================
  # 3. ATTACK FUNCTION (Stochastic + Dual Eval)
  # ==============================================================================

--- File: Untitled-1.py ---
IMPORTS (Libraries used):
import os
import shutil
import time
import numpy as np
import matplotlib.pyplot as plt
import torch
import torch.nn as nn
import torch.optim as optim
import torchvision.transforms as T
from torchvision import datasets, models, transforms
from torch.utils.data import DataLoader

CLASSES:
  class ReIDNormalize(nn.Module):
  class ReIDUnNormalize(nn.Module):
  class ModelB_ResNet50(nn.Module):

FUNCTIONS/METHODS:
  def seed_everything(seed=42)
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor - self.mean) / self.std
  def __init__(self, mean, std)
  def forward(self, tensor): return (tensor * self.std) + self.mean
  def __init__(self, num_classes=751)
  def forward(self, x): return self.model(x)
  def get_embedding(self, x)
  def get_images_by_pid(dataset, target_pid, max_images=50)
  def extract_features_robust(model, dataloader, device)
  def get_id_and_cam(dataset)
  def evaluate_attack_full_metrics(model, dataloaders, pattern, mask, target_pid, query_images, device)
  def calc_metrics_batch(q_feats)

KEY CONSTANTS/CONFIG:
  DATA_ROOT = './Market-1501-v15.09.15'  # Ensure this path is correct

KEY CODE COMMENTS:
  # 1. Configuration
  # 2. Reproducibility
  # 3. Physics-Aware Normalization Helpers
  # These are crucial for applying the pattern in "Real World" pixel space (0-1)
  # Define Standard ImageNet Normalization Layers
  # --- 1. DATA CONFIGURATION ---
  # --- 2. VICTIM MODEL (ResNet-50 IDE) ---
  # Extract features before the final layer
  # Initialize and Load Weights
  # 1. Gallery Features============================================================
=== REPO: CourtSense-AI ===
URL: https://github.com/udayraj1238/CourtSense-AI
Description: No description
Primary Language: Python | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: backend/main.py ---
IMPORTS (Libraries used):
from fastapi import FastAPI, HTTPException, UploadFile, File
from fastapi.middleware.cors import CORSMiddleware
from models import SequenceResponse, FrameData, BallState, Coordinate, PlayerState
import numpy as np
import json
import os
import shutil
import tempfile
import sys
from scripts.process_real_video import process_video

FUNCTIONS/METHODS:
  def health_check()
  async def upload_and_process_video(file: UploadFile = File(...)) -- Receives a video file, processes it synchronously using the CV pipeline, and returns the 3D tracking sequence data.
  def get_mock_tracking_sequence() -- Returns a sequence of mocked tracking data mirroring Phase 4's Kalman filter trajectory generation.
  def get_real_tracking_sequence() -- Returns the tracking sequence processed from the real video by process_real_video.py.

KEY CODE COMMENTS:
  # Add scripts directory to path to import process_real_video
  # Configure CORS for the frontend
  # Save uploaded file to a temporary location
  # Process the video (this will block the thread for a while)
  # Clean up temp file
  # Clean up temp file on error
  # Parabola parameters (mirroring phase4_trajectory.py)
  # Simulate tracking depth moving slightly across the court
  # Two mock players (Ghost Phase representations)

--- File: colab_backend.ipynb ---
[NOTEBOOK DOC]: # CourtSense AI - Free Online Backend

This notebook allows you to run the CourtSense AI backend for free using Google Colab's cloud servers.

**Instructions:**
1. In the menu, go to **Runtime > Change runtime type** and ensure Hardware accelerator is set to **T4 GPU** (or CPU if GPU is unavailable, though GPU is much faster for YOLOv8).
2. Click the "Play" button on each cell below (or go to **Ru

[CODE CELL 1]:
# 1. Clone the repository and install requirements
# We need this for OpenCV in colab

[CODE CELL 2]:
import urllib.request
# 2. Run the FastAPI server in the background 
# 3. Open Localtunnel endpoint (Copy the your url is: ... link!)

--- File: README.md ---
<div align="center">
  <img src="https://img.icons8.com/color/96/000000/tennis.png" alt="Tennis Icon" width="80" />
  
  # CourtSense AI 🎾
  
  **A Next-Generation 3D Tennis Analytics Engine**

  <p>
    <a href="https://udayraj1238.github.io/CourtSense-AI/">
      <img src="https://img.shields.io/badge/Live_Demo-Play_Now-a3e635?style=for-the-badge&logo=vercel&logoColor=black" alt="Live Demo" />
    </a>
  </p>

  <p>
    <img src="https://img.shields.io/badge/Python-3.10+-blue?style=flat-square&logo=python" alt="Python" />
    <img src="https://img.shields.io/badge/React-18-61dafb?style=flat-square&logo=react&logoColor=black" alt="React" />
    <img src="https://img.shields.io/badge/Three.js-3D-black?style=flat-square&logo=three.js" alt="Three.js" />
    <img src="https://img.shields.io/badge/YOLOv8-Pose-ff0000?style=flat-square" alt="YOLOv8" />
  </p>
</div>

---

**CourtSense AI** processes standard tennis match videos using computer vision and transforms them into stunning, interactive 3D visualizations. Witness every rally, track player movement, and analyze ball physics in real-time.

✨ **[Experience the Live 3D Viewer Here](https://udayraj1238.github.io/CourtSense-AI/)** ✨

---

## 🌟 Key Features

- 🏟️ **Cinematic 3D Court** — Immersive real-time tennis stadium with lighting, dynamic shadows, and post-processing (Bloom/Vignette) rendered using `react-three-fiber`.
- 🏃 **Player Tracking & Ghosting** — Real-world player detection using YOLOv8-Pose, mapped to 3D space via homography projection.
- 🎾 **Physics-Based Ball Trajectory** — Smooth, lag-free ball tracking with Kalman filters, Magnus effect correction, and a dynamic "comet tail" visualizer.
- 📊 **Live Analytics Dashboard** — Real-time display of ball speed (km/h), spin rate (rpm), and frame-accurate playback.
- 🎥 **Interactive Playback** — Glassmorphic UI with timeline scrubbing, variable playback speed (0.25x - 2x), and smart camera presets (TV, Top-down, Player POV).
- 🧠 **AI Court Segmentation** — SegFormer-based court surface detection and automatic camera calibration.

---

## 🛠️ Technology Stack

| Layer | Technology |
|---|---|
| **🎨 Frontend** | React, Three.js (`react-three-fiber`), TailwindCSS, Vanilla CSS Design System |
| **⚙️ Backend** | FastAPI, Uvicorn, Python |
| **👁️ Computer Vision** | YOLOv8-Pose (Ultralytics), OpenCV, NumPy |
| **🧮 Physics & Math** | Kalman Filters, Magnus Effect Modeling, EMA Smoothing |
| **🤖 Machine Learning** | SegFormer (HuggingFace) |

---

## 🚀 Quick Start: Process Your Own Videos

### Option A: Free Online Processing (No Installation!)
You can process custom, real-world videos directly on the Live Site for completely free using our automated Google Colab backend.

1. **Deploy your AI Backend:** 
   - Open the **[CourtSense Colab Backend](https://colab.research.google.com/github/udayraj1238/CourtSense-AI/blob/main/colab_backend.ipynb)** notebook.
   - In the top menu, click **Runtime > Run all**.
   - Scroll to the very bottom and wait ~30 s

--- File: frontend/README.md ---
# React + TypeScript + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) (or [oxc](https://oxc.rs) when used in [rolldown-vite](https://vite.dev/guide/rolldown)) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend updating the configuration to enable type-aware lint rules:

```js
export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...

      // Remove tseslint.configs.recommended and replace with this
      tseslint.configs.recommendedTypeChecked,
      // Alternatively, use this for stricter rules
      tseslint.configs.strictTypeChecked,
      // Optionally, add this for stylistic rules
      tseslint.configs.stylisticTypeChecked,

      // Other configs...
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```

You can also install [eslint-plugin-react-x](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x) and [eslint-plugin-react-dom](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom) for React-specific lint rules:

```js
// eslint.config.js
import reactX from 'eslint-plugin-react-x'
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```

--- File: backend/models.py ---
IMPORTS (Libraries used):
from pydantic import BaseModel
from typing import List, Optional

CLASSES:
  class Coordinate(BaseModel):
  class PlayerState(BaseModel):
  class BallState(BaseModel):
  class FrameData(BaseModel):
  class SequenceResponse(BaseModel):

KEY CODE COMMENTS:
  # Potential future biometric data
  # joint_angles: dict

--- File: courtsense_ai/__init__.py ---
KEY CODE COMMENTS:
  # courtsense_ai - Tennis Analytics Engine

--- File: courtsense_ai/core/__init__.py ---
KEY CODE COMMENTS:
  # Core modules: calibration, segmentation, tracking, physics

--- File: courtsense_ai/core/ball_tracking.py ---
IMPORTS (Libraries used):
import numpy as np
import cv2
from typing import Optional, Tuple
from ultralytics import YOLO

CLASSES:
  class BallTracker:

FUNCTIONS/METHODS:
  def __init__(self, model_size: str = 'yolov8n.pt') -- Initializes the YOLOv8 object detection model for generic tracking. In a production tennis system, this would be a custom-finetuned TrackNet or ResNet specifically trained on tennis balls (e.g., custom weights 'tennis_ball_tracker.pt').
  def detect_ball(self, frame: np.ndarray) -> Optional[np.ndarray] -- Runs object detection to find the tennis ball in the frame.  Args: frame: Input BGR image (H, W, 3).

KEY CODE COMMENTS:
  # Find all sports balls
  # Find the one with the highest confidence
  # We want the center of the bounding box

--- File: courtsense_ai/core/biometrics.py ---
IMPORTS (Libraries used):
import numpy as np
import cv2
from typing import List, Dict, Any
from ultralytics import YOLO

CLASSES:
  class PoseEstimator:

FUNCTIONS/METHODS:
  def __init__(self, model_size: str = 'yolov8n-pose.pt') -- Initializes the YOLOv8 Pose model. Args: model_size (str): e.g., 'yolov8n-pose.pt', 'yolov8s-pose.pt'
  def extract_keypoints(self, frame: np.ndarray) -> List[Dict[str, Any]] -- Runs pose estimation on an input frame.  Args: frame: Input BGR image (H, W, 3).
  def calculate_joint_angle(p1: np.ndarray, p2: np.ndarray, p3: np.ndarray) -> float -- Calculates the 2D angle (in degrees) formed by three keypoints: p1-p2-p3 (p2 is the vertex). E.g., p1=Shoulder, p2=Elbow, p3=Wrist.  Args:

KEY CODE COMMENTS:
  # Automatically downloads weights if they don't exist
  # ultralytics uses BGR by default, but double-check if image comes from PIL
  # keypoints.xy contains (x, y) coordinates for the 17 joints
  # Check if any point was not detected (YOLO might output 0,0 for occluded points)
  # Dot product method
  # Clip to handle floating point inaccuracies

--- File: courtsense_ai/core/camera_calibration.py ---
IMPORTS (Libraries used):
from the camera coordinate space (pixel plane) to a top-down metric world space.
import cv2
import numpy as np
from typing import Tuple

FUNCTIONS/METHODS:
  def get_homography_matrix(src_points: np.ndarray, dst_points: np.ndarray) -> Tuple[np.ndarray, np.ndarray] -- Computes the Homography Matrix H that maps points from the source plane (image pixels) to the destination plane (real-world top-down metric view).  Mathematical Derivation:
  def project_points(points: np.ndarray, homography_matrix: np.ndarray) -> np.ndarray -- Projects an array of 2D points from pixel space to world space using Homography.  Args: points (np.ndarray): Array of points to transform. Shape: (N, 1, 2) or (N, 2).

KEY CODE COMMENTS:
  # Ensure points shape is (N, 1, 2) for cv2.perspectiveTransform
  # Reshape (N, 2) -> (N, 1, 2)

--- File: courtsense_ai/core/physics.py ---
IMPORTS (Libraries used):
import numpy as np
import cv2

CLASSES:
  class TrajectoryPredictor:

FUNCTIONS/METHODS:
  def __init__(self, dt: float = 1/30.0) -- Initializes a Constant Acceleration Kalman Filter state estimator. State: [x, y, vx, vy, ax, ay]^T Measurement: [x, y]^T
  def update(self, measurement: np.ndarray) -> np.ndarray -- Updates the Kalman filter with a new [x, y] observation.  Args: measurement (np.ndarray): [x, y] coordinate detected by CNN.
  def predict(self) -> np.ndarray -- Predicts the next state (useful during occlusion when CNN fails).  Returns: np.ndarray: Predicted [x, y] coordinate.
  def apply_magnus_correction(self, state_y: float, spin_factor: float = 0.5) -> float -- Applies a domain-specific correction based on the Magnus effect (topspin/slice) to the predicted trajectory's Y-axis (gravity/drop).  Topspin creates a downward force (ball drops faster).

KEY CODE COMMENTS:
  # Measurement Matrix H (we only measure x and y)
  # State Transition Matrix F (Constant Acceleration Kinematics)
  # x_new = x + vx*dt + 0.5*ax*dt^2
  # Process Noise Covariance Q (trust the model less for acceleration)
  # Measurement Noise Covariance R (trust the YOLO detector)
  # Initial State Error Covariance P
  # Initialize state with first measurement
  # Predict followed by Update
  # In a real model, this would augment the Acceleration (ay) state dynamically.
  # Here we simulate the effect by increasing the dip directly if topspin > 0.

--- File: courtsense_ai/core/segmentation.py ---
IMPORTS (Libraries used):
import numpy as np
from PIL import Image
import torch
from transformers import SegformerImageProcessor, SegformerForSemanticSegmentation
from typing import Dict

CLASSES:
  class CourtSegmenter:

FUNCTIONS/METHODS:
  def __init__(self, model_id: str = "nvidia/segformer-b2-finetuned-cityscapes-1024-1024") -- Initializes the SegFormer model and processor for semantic segmentation. Uses a Cityscapes-finetuned model as a placeholder/baseline. A proper tennis-court-finetuned model would be ideal in production.
  def segment_frame(self, frame: np.ndarray) -> Dict[str, np.ndarray] -- Runs SegFormer inference on an input frame and returns masks.  Args: frame (np.ndarray): Input video frame (RGB, HxWxC, uint8).

KEY CODE COMMENTS:
  # Convert OpenCV/numpy array to PIL Image
  # Post-process logits to masks
  # Resize logits to original image size
  # Get segmentation mask (argmax over class dimension)
  # NOTE: A real CourtSense model needs specific classes (Court, Lines, Net, Players).
  # Since we use cityscapes placeholder, we mock the extraction logic here:
  # e.g., Road -> Court (approx), Person -> Player.============================================================
=== REPO: Pytorch_PaliGemma ===
URL: https://github.com/udayraj1238/Pytorch_PaliGemma
Description: No description
Primary Language: Python | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: modeling_siglip.py ---
IMPORTS (Libraries used):
from typing import Optional, Tuple
import torch
import torch.nn as nn

CLASSES:
  class SiglipVisionConfig:
  class SiglipVisionEmbeddings(nn.Module):
  class SiglipAttention(nn.Module):: Multi-headed attention from 'Attention Is All You Need' paper

FUNCTIONS/METHODS:
  def __init__(
  def __init__(self, config: SiglipVisionConfig)
  def forward(self, pixel_values: torch.FloatTensor) -> torch.Tensor
  def __init__(self, config)
  def forward(

KEY CODE COMMENTS:
  # Convolve the `patch_size` kernel over the image, with no overlapping patches since the stride is equal to the kernel size
  # The output of the convolution will have shape [Batch_Size, Embed_Dim, Num_Patches_H, Num_Patches_W]
  # where Num_Patches_H = height // patch_size and Num_Patches_W = width // patch_size
  # [Batch_Size, Embed_Dim, Num_Patches_H, Num_Patches_W] -> [Batch_Size, Embed_Dim, Num_Patches]
  # where Num_Patches = Num_Patches_H * Num_Patches_W
  # [Batch_Size, Embed_Dim, Num_Patches] -> [Batch_Size, Num_Patches, Embed_Dim]
  # Add position embeddings to each patch. Each positional encoding is a vector of size [Embed_Dim]
  # [Batch_Size, Num_Patches, Embed_Dim]
  # hidden_states: [Batch_Size, Num_Patches, Embed_Dim]
  # query_states: [Batch_Size, Num_Patches, Embed_Dim]

--- File: modelling_gemma.py ---
IMPORTS (Libraries used):
import torch
from torch import nn
from typing import Optional, Tuple, List
from torch.nn import CrossEntropyLoss
import math
from modeling_siglip import SiglipVisionConfig, SiglipVisionModel

CLASSES:
  class KVCache():
  class GemmaConfig():
  class PaliGemmaConfig():
  class GemmaRMSNorm(nn.Module):
  class GemmaRotaryEmbedding(nn.Module):

FUNCTIONS/METHODS:
  def __init__(self) -> None
  def num_items(self) -> int
  def update(
  def __init__(
  def __init__(
  def __init__(self, dim: int, eps: float = 1e-6)
  def _norm(self, x)
  def forward(self, x)
  def __init__(self, dim, max_position_embeddings=2048, base=10000, device=None)
  def forward(self, x, position_ids, seq_len=None)

KEY CODE COMMENTS:
  # The shape of the key_cache is [Batch_Size, Num_Heads_KV, Seq_Len, Head_Dim]
  # If we never added anything to the KV-Cache of this layer, let's create it.
  # ... otherwise we concatenate the new keys with the existing ones.
  # each tensor has shape: [Batch_Size, Num_Heads_KV, Seq_Len, Head_Dim]
  # ... and then we return all the existing keys + the new ones.
  # Llama does x.to(float16) * w whilst Gemma is (x * w).to(float16)
  # See https://github.com/huggingface/transformers/pull/29402
  # Calculate the theta according to the formula theta_i = base^(-2i/dim) where i = 0, 1, 2, ..., dim // 2
  # x: [bs, num_attention_heads, seq_len, head_size]
  # Copy the inv_freq tensor for batch in the sequence

--- File: processing_paligemma.py ---
IMPORTS (Libraries used):
from typing import Dict, List, Optional, Union, Tuple, Iterable
import numpy as np
from PIL import Image
import torch

CLASSES:
  class PaliGemmaProcessor:

FUNCTIONS/METHODS:
  def add_image_tokens_to_prompt(prefix_prompt, bos_token, image_seq_len, image_token)
  def rescale(
  def resize(
  def normalize(
  def process_images(
  def __init__(self, tokenizer, num_image_tokens: int, image_size: int)
  def __call__(

KEY CONSTANTS/CONFIG:
  IMAGENET_STANDARD_MEAN = [0.5, 0.5, 0.5]
  IMAGENET_STANDARD_STD = [0.5, 0.5, 0.5]
  IMAGE_TOKEN = "<image>"
  EXTRA_TOKENS = [

KEY CODE COMMENTS:
  # Quoting from the blog (https://huggingface.co/blog/paligemma#detailed-inference-process):
  # The input text is tokenized normally.
  # A <bos> token is added at the beginning, and an additional newline token (\n) is appended.
  # This newline token is an essential part of the input prompt the model was trained with, so adding it explicitly ensures it's always there.
  # The tokenized text is also prefixed with a fixed number of <image> tokens.
  # NOTE: from the paper it looks like the `\n` should be tokenized separately, but in the HF implementation this is not done.
  # ref to HF implementation: https://github.com/huggingface/transformers/blob/7f79a97399bb52aad8460e1da2f36577d5dccfed/src/transformers/models/paligemma/processing_paligemma.py#L55-L73
  # Convert each image to a numpy array
  # Rescale the pixel values to be in the range [0, 1]
  # Normalize the images to have mean 0 and standard deviation 1============================================================
=== REPO: Transformer_from_scratch_using_pytorch ===
URL: https://github.com/udayraj1238/Transformer_from_scratch_using_pytorch
Description: No description
Primary Language: Jupyter Notebook | Stars: 0

CODE ANALYSIS (from actual source files):
--- File: Transformer_from_scratch_using_pytorch/model.py ---
IMPORTS (Libraries used):
import torch
import torch.nn as nn
import math

CLASSES:
  class InputEmbeddings(nn.Module):
  class PositionalEncoding(nn.Module):
  class LayerNormalization(nn.Module):
  class FeedForwardBlock(nn.Module):
  class MultiHeadAttentionBlock(nn.Module):
  class ResidualConnection(nn.Module):
  class EncoderBlock(nn.Module):

FUNCTIONS/METHODS:
  def __init__(self , d_model: int , vocab_size: int)
  def forward(self , x)
  def __init__(self , d_model: int , seq_len: int, dropout: float) -> None
  def forward(self , x)
  def __init__(self , eps : float = 10**-6) -> None
  def forward(self , x)
  def __init__(self , d_model : int , d_ff : int , dropout : float) -> None
  def forward(self , x)
  def __init__(self, d_model: int, h: int, dropout: float) -> None
  def attention(query, key, value, mask, dropout: nn.Dropout)
  def forward(self, q, k, v, mask)
  def __init__(self, features: int, dropout: float) -> None
  def forward(self, x, sublayer)
  def __init__(self, features: int, self_attention_block: MultiHeadAttentionBlock, feed_forward_block: FeedForwardBlock, dropout: float) -> None
  def forward(self, x, src_mask)

KEY CODE COMMENTS:
  # create a matrix of shape (seq_len , d_model)
  # create a vector of length (seq_len)
  # Apply the sin to even positions
  # (Batch , Seq_len , d_model) ---> (Batch , Seq_len , d_ff) ---> (batch , seq_len , d_model)
  # Make sure d_model is divisible by h
  # Just apply the formula from the paper
  # (batch, h, seq_len, d_k) --> (batch, h, seq_len, seq_len)
  # Write a very low value (indicating -inf) to the positions where mask == 0
  # (batch, h, seq_len, seq_len) --> (batch, h, seq_len, d_k)
  # return attention scores which can be used for visualization

--- File: Transformer_from_scratch_using_pytorch/train.py ---
IMPORTS (Libraries used):
from model import build_transformer
from dataset import BilingualDataset, causal_mask
from config import get_config, get_weights_file_path, latest_weights_file_path
import torch
import torch.nn as nn
from torch.utils.data import Dataset, DataLoader, random_split
from torch.optim.lr_scheduler import LambdaLR
import warnings
from tqdm import tqdm
import os
from pathlib import Path
from datasets import load_dataset
from tokenizers import Tokenizer
from tokenizers.models import WordLevel
from tokenizers.trainers import WordLevelTrainer
from tokenizers.pre_tokenizers import Whitespace
import torchmetrics
from torch.utils.tensorboard import SummaryWriter

FUNCTIONS/METHODS:
  def greedy_decode(model, source, source_mask, tokenizer_src, tokenizer_tgt, max_len, device)
  def run_validation(model, validation_ds, tokenizer_src, tokenizer_tgt, max_len, device, print_msg, global_step, writer, num_examples=2)
  def get_all_sentences(ds, lang)
  def get_or_build_tokenizer(config, ds, lang)
  def get_ds(config)

KEY CODE COMMENTS:
  # Huggingface datasets and tokenizers
  # Precompute the encoder output and reuse it for every step
  # Initialize the decoder input with the sos token
  # build mask for target
  # calculate output
  # get the console window width
  # If we can't get the console width, use 80 as default
  # check that the batch size is 1
  # Print the source, target and model output
  # Evaluate the character error rate